问：
请你扮演数学/物理/计算机/博弈论/实验设计/自驱动实验的专家，在上面这些痛点，别人做的不够好的基础上，给我提供3个足够创新的解决方案，而且要从底层，从数学，从物理，从计算机的角度，从智能体/技术的角度去提，别跟我搞方案，材料性能，材料性能是结果不是创新性。聚焦于自驱动实验室，Agent（Harness engineering，记忆（多Agent记忆、记忆压缩等等），skills）。

回答：
# 核心判断

真正值得创新的，不是再加一个大模型、更多 Agent 或更多自动化模块，而是建立三种目前仍缺失的**底层科学语义**：

| 研究命题                    | 要解决的根问题                  | 主要突破对象               |
| ----------------------- | ------------------------ | -------------------- |
| **1. 物理信念状态科学 Harness** | 物理动作什么时候算发生？测量什么时候能成为证据？ | Harness、物理状态、形式化执行   |
| **2. 反事实证明携带型科学记忆**     | 系统可以忘掉什么，而不改变科学结论？       | 多Agent记忆、失效传播、记忆压缩   |
| **3. 可认证科学技能与双账户认知博弈**  | 一个Skill在什么条件下有资格自主执行？    | Skills、动态自治、博弈论、主动验证 |

截至 **2026年6月22日**，NIST仍明确表示材料自主实验尚不存在标准化生态，当前标准化工作仍集中在样品管理、仪器通信、数据知识管理以及算法模型集成。最新的LAP已经提出仪器锁、物理单位、校准、不确定度和不可逆操作等语义，但它仍是v0.1设计规范，没有规范化实现和大规模实证验证。现有SDL评价主要关注自主度、运行时间、精度、吞吐量、成本和优化效率；ADePT则评价机器人适应性、灵巧性、感知和任务复杂度。它们都还没有解决“物理动作—有效测量—科学主张”之间的形式化提交问题。([NIST][1])

下面三个不是一般的软件架构建议，而是可以分别形成论文、专利或博士研究方向的**底层研究命题**。

---

# 一、三重提交的物理信念状态科学 Harness

## Tri-Commit Belief-State Scientific Harness

## 1. 根本问题

普通Agent Harness中的世界通常是：

[
\text{状态}+\text{工具调用}+\text{返回结果}
]

但真实湿实验中的世界是：

[
\text{不可见物理状态}
\xrightarrow{\text{不可逆动作}}
\text{新的不可见状态}
\xrightarrow{\text{带误差观测}}
\text{测量结果}
\xrightarrow{\text{计量判断}}
\text{科学证据}
]

仪器返回`success`，最多只能证明软件命令执行完毕，不能证明：

* 物理操作确实按预期发生；
* 样品状态没有偏离；
* 测量满足有效性条件；
* 结果可以更新模型；
* 结果能够支撑某个科学主张。

现有Agent工具安全研究已经开始使用时序逻辑、运行时验证和shield拦截不安全动作，但主要关注工具序列、数据流和行为约束，而不是湿实验中的潜在物理状态和测量有效性。([arXiv][2])

---

## 2. 数学底层：把实验室建模为随机混合POMDP

定义系统真实状态：

[
\Xi_t=
\left(
x_t^{\mathrm{phys}},
q_t^{\mathrm{workflow}},
\theta_t^{\mathrm{measurement}},
G_t^{\mathrm{claim}}
\right)
]

其中：

* (x_t^{\mathrm{phys}})：样品、环境、接触、热史、仪器内部状态；
* (q_t^{\mathrm{workflow}})：协议执行到哪一步；
* (\theta_t^{\mathrm{measurement}})：校准、漂移、噪声模型和测量有效性；
* (G_t^{\mathrm{claim}})：当前科学主张和证据依赖状态。

Agent不能直接观察 (\Xi_t)，只能得到：

[
o_t\sim p(o_t\mid \Xi_t,a_{t-1})
]

因此Harness维护的不是一个数据库字段，而是一个信念分布：

[
b_t(\Xi)=
p(\Xi_t=\Xi\mid o_{0:t},a_{0:t-1})
]

湿实验同时具有连续状态和离散状态：

[
x_{t+1}=f_{q_t}(x_t,u_t,w_t)
]

[
q_{t+1}=\delta(q_t,a_t)
]

所以更准确的数学对象是：

> **随机混合系统 + POMDP + 形式化状态机。**

近期针对真实SDL的研究也开始将其视为具有昂贵动作、噪声反馈、非平稳性和严格约束的Agent–环境交互问题，但还没有形成物理状态、计量状态和主张状态的统一事务语义。

---

## 3. 核心原创：三重科学提交

每一次实验操作不再只有“成功/失败”，而是经过三个不可跳过的提交层。

### 第一层：物理提交 Physical Commit

确认某个物理动作究竟发生了什么。

[
C_P(a_t)=1
]

并不要求“操作符合预期”，只要求系统能够确定：

* 动作已发生；
* 动作未发生；
* 动作部分发生；
* 当前无法确定。

如果仪器通信超时，系统首先进行状态核对，而不是直接重试。

---

### 第二层：计量提交 Metrological Commit

判断观测是否满足预先声明的测量资格：

[
C_M(y_t)=
\mathbf 1
\left[
Q(y_t,\theta_t)\in \mathcal Q_{\mathrm{valid}}
\right]
]

例如：

* 校准是否有效；
* 环境是否稳定；
* 测量是否达到平衡；
* 时间漂移是否超限；
* 重复性是否满足要求；
* 信号是否具有足够可辨识性；
* 不确定度是否可计算。

---

### 第三层：认知提交 Epistemic Commit

只有经过计量提交的观测，才允许进入模型和主张系统：

[
C_E(y_t)=1
\iff
C_P=1
\land C_M=1
\land \operatorname{scope}(y_t)\text{明确}
]

核心不变量是：

[
C_E(y_t)=0
\Rightarrow
y_t\notin
\left{
D_{\mathrm{BO}},
D_{\mathrm{claim}},
D_{\mathrm{skill\ update}}
\right}
]

即：

> **无效测量可以用于故障诊断，但绝不能悄悄进入科学模型、BO或长期事实记忆。**

这是方案一最重要的创新点。

---

## 4. 物理底层：用不可逆偏序代替“回滚”

软件系统假设：

[
a^{-1}(a(x))=x
]

湿实验通常不满足这个条件。样品被干燥、升降温、吸湿、装夹、老化或反复测量后，状态一般不能恢复到此前状态。

因此定义不可逆状态偏序：

[
x_t\preceq x_{t+1}
]

其中“更大”不代表更好，而代表累积了更多不可消除的物理历史。

恢复操作不是逆操作，而是：

[
a_{\mathrm{comp}}:
x_{\mathrm{failed}}
\rightarrow
\mathcal X_{\mathrm{safe}}
]

例如：

* 重新达到稳定状态；
* 将样品隔离；
* 创建新样品分支；
* 降低结论等级；
* 终止当前实验轨迹；
* 将后续结果标记为另一实验条件。

因此Harness的事务语义应从数据库的rollback改为：

> **reconcile → compensate → forward recover → quarantine**

即：

> 状态核对、补偿、前向恢复和隔离。

---

## 5. 实验设计底层：同时测“科学问题”和“系统状态”

传统主动学习选择：

[
a^*=
\arg\max_a I(H;Y_a\mid D)
]

只关心实验对科学假设 (H) 的信息量。

但部分可观测湿实验还必须识别当前物理状态 (X)。因此建议使用双信息增益：

[
a^*=
\arg\max_a
\left[
I(H;Y_a\mid D)
+
\lambda I(X;Y_a\mid D)
----------------------

## \alpha C(a)

\beta \operatorname{CVaR}_{\gamma}(L_a)
\right]
]

其中：

* 第一项：这个实验能否区分科学假设；
* 第二项：这个实验能否判断样品或仪器当前处于什么状态；
* (C(a))：时间、样品和人工成本；
* (\operatorname{CVaR})：尾部风险；
* (\lambda)：当前更需要发现还是诊断。

因此某些实验的价值不是产生更好的结果，而是回答：

> “当前异常到底来自材料行为、测量漂移、样品热史还是执行失败？”

对于环境分布不确定的实验，可以进一步使用分布鲁棒机会约束，而不是假定环境变量服从固定已知分布。

---

## 6. 计算机底层

Harness内核由以下四种确定性机制组成：

| 机制                  | 作用                  |
| ------------------- | ------------------- |
| 事件溯源 Event Sourcing | 所有物理和认知状态由不可变事件重建   |
| 状态核对器 Reconciler    | 超时或故障后判断动作是否实际发生    |
| 时序逻辑Shield          | 在动作执行前检查锁、前置状态和安全条件 |
| 科学提交控制器             | 控制数据能否进入模型、记忆和主张    |

可以形式化验证以下性质：

### 证据隔离性

[
\Box
\left(
\neg C_E(y)
\rightarrow
\neg\operatorname{updateModel}(y)
\right)
]

### 禁止盲目重试

[
\Box
\left(
\operatorname{timeout}(a)
\rightarrow
\neg\operatorname{retry}(a)
\ \mathcal U
\operatorname{reconciled}(a)
\right)
]

### 锁与状态约束

[
\Box
\left(
\operatorname{execute}(a)
\rightarrow
\operatorname{reserved}
\land
\operatorname{authorized}
\land
\operatorname{preconditionSatisfied}
\right)
]

---

## 7. 可以追求的理论结果

若每个操作在当前信念状态下的条件风险满足：

[
P(F_i\mid F_1^c,\ldots,F_{i-1}^c)\le\delta_i
]

则整个工作流的失败概率可以上界为：

[
P\left(\bigcup_{i=1}^{n}F_i\right)
\le
\sum_{i=1}^{n}\delta_i
]

还可以证明：

1. 未通过认知提交的数据不可能污染BO和科学主张；
2. 同一事件账本可以确定性重建相同科学状态；
3. 多Agent不能同时修改同一样品的权威状态；
4. 未经状态核对的失败调用不能自动重试；
5. 风险预算可随工作流组合传播。

---

## 8. 真正评价指标

这个研究不评价电导率，而评价：

* **错误认知提交率**：无效观测进入科学模型的比例；
* **物理状态后验覆盖率**；
* **超时后错误重复操作率**；
* **故障恢复后状态一致率**；
* **机会约束违反率**；
* **科学状态可回放率**；
* **无效证据泄漏率**；
* **同等实验预算下的状态辨识增益**。

---

# 二、反事实证明携带型科学记忆

## Counterfactual Proof-Carrying Scientific Memory

## 1. 根本问题

当前Agent记忆主要优化：

[
\text{能不能检索回来}
]

或：

[
\text{能不能用更少token保留任务相关信息}
]

最新研究已经分别开始用信息瓶颈优化记忆压缩、用率失真理论保存决策、用溯源图评估证据失效级联。但这些工作仍然是分开的：信息瓶颈不保证保留反证条件；决策保真不必然保留科学主张等级；证据失效基准也没有成为真实湿实验中的权威记忆协议。

你们真正需要的问题是：

> **记忆压缩或更新以后，如果某个证据被作废，系统能否得到与未压缩历史完全相同的结论降级结果？**

这比“还能不能回答一个问题”严格得多。

---

## 2. 记忆不再是文本，而是证据超图

定义科学记忆：

[
G=(V,E)
]

节点包括：

[
V=
V_{\mathrm{raw}}
\cup
V_{\mathrm{observation}}
\cup
V_{\mathrm{assumption}}
\cup
V_{\mathrm{model}}
\cup
V_{\mathrm{claim}}
\cup
V_{\mathrm{skill}}
]

边包括：

* generated-by；
* derived-from；
* supports；
* refutes；
* depends-on；
* valid-under；
* invalidates；
* supersedes；
* executed-by；
* produced-with-skill-version。

W3C PROV已经提供Entity、Activity和Agent的基础溯源结构，但它不直接定义科学主张、反证、适用域和证据失效传播，因此需要在其上构建科学语义层。([W3C][3])

---

## 3. 用四值逻辑保存科学冲突

对每个主张 (c)，记录两个独立维度：

[
v(c)=
\left(
s(c),r(c)
\right)
]

其中：

* (s(c)=1)：存在有效支持链；
* (r(c)=1)：存在有效反驳链。

得到四种状态：

| 数值      | 科学状态                   |
| ------- | ---------------------- |
| ((0,0)) | Unknown / Inconclusive |
| ((1,0)) | Supported              |
| ((0,1)) | Refuted                |
| ((1,1)) | Contested              |

这是一种适用于科学记忆的容矛盾逻辑。

它解决一个非常重要的问题：

> 正证据和反证据同时存在时，系统不能投票、覆盖旧结论或强行选择一方，而应保留“Contested”状态。

---

## 4. 用最小支持集实现证据失效传播

每个主张不是绑定一条解释文本，而是绑定若干最小支持集：

[
\mathcal J^+(c)
===============

\left{
J^+_1,J^+_2,\ldots,J^+_k
\right}
]

其中每个 (J^+_i) 都是足以支持主张 (c) 的最小证据组合。

反驳也类似：

[
\mathcal J^-(c)
===============

\left{
J^-_1,J^-_2,\ldots,J^-_m
\right}
]

当证据 (e) 被作废时：

[
G'=\mathcal I_e(G)
]

删除所有包含 (e) 的支持路径，但保留不依赖 (e) 的其他路径。

因此：

[
c\text{仍被支持}
\iff
\exists J_i^+(c):
J_i^+(c)
\subseteq E_{\mathrm{valid}}
]

这可以保证：

* 依赖失效数据的结论被自动降级；
* 有独立支持的结论不会被错误删除；
* 旧结论无法继续通过摘要或RAG反复污染新推理；
* 可以精确回答“如果去掉某项证据，哪些结论仍成立”。

RECON基准已经说明，证据失效需要通过溯源图动态计算下游影响，而不能把失效结论列表写死。你们可以把这一思想从记忆基准推进到真实实验的权威记忆内核。

---

## 5. 多Agent记忆不能使用同一种一致性

不同科学记忆需要不同的一致性协议。

| 记忆类型       | 一致性机制              |
| ---------- | ------------------ |
| 原始数据和新观测   | Append-only / CRDT |
| 单一样品的物理状态  | 单写者租约 + MVCC       |
| 协议执行状态     | 强一致状态机             |
| Agent草稿与假设 | 最终一致即可             |
| 科学主张状态     | 可串行化提交             |
| 技能版本与权限    | 强版本一致性             |
| 已作废证据      | 全局失效广播             |

关键原则是：

> **观测可以并发追加，科学主张不能最后写入者胜出。**

Agent A支持某主张、Agent B反驳某主张时，不能用：

* 谁最后写入；
* 谁模型更大；
* 谁语言更自信；
* 多数投票。

而应产生一个带版本的`Contested`节点，并触发下一次判别实验。

---

## 6. 核心原创：反事实科学率失真压缩

现有率失真记忆的核心思想是：

> 压缩后只要不改变Agent决策，就可以遗忘其他信息。

但科学系统还必须保留：

* 主张状态；
* 主张等级；
* 适用条件；
* 反证；
* 可推翻条件；
* 证据失效后的级联结果。

定义完整历史为 (H)，压缩记忆为 (M)，科学查询集合为 (\mathcal Q)。

提出科学失真：

[
D_{\mathrm{sci}}(H,M)
=====================

\sup_{q\in\mathcal Q}
\Big[
\alpha
d_A\big(
\pi_H(q),\pi_M(q)
\big)
+
\beta
d_C\big(
\chi_H(q),\chi_M(q)
\big)
+
\gamma
D_{\mathrm{cf}}(q)
\Big]
]

其中：

* (\pi)：下一实验或下一动作决策；
* (\chi)：科学主张状态；
* (d_A)：动作差异；
* (d_C)：主张等级或状态差异；
* (D_{\mathrm{cf}})：反事实失真。

反事实失真定义为：

[
D_{\mathrm{cf}}(q)
==================

\max_{e\in\mathcal E_{\mathrm{critical}}}
d_C
\left(
\chi_{\mathcal I_e(H)}(q),
\chi_{\mathcal I_e(M)}(q)
\right)
]

这要求：

> 压缩后的记忆不仅要在当前证据下得出相同结论，在任意关键证据被撤销后，也必须产生相同的结论降级。

优化目标：

[
\min_M R(M)
]

约束：

[
D_{\mathrm{sci}}(H,M)\le\epsilon
]

其中 (R(M)) 是token、存储和检索成本。

这就是：

> **反事实决策保真压缩，而不是文本摘要。**

---

## 7. 零失真信息

以下信息不能有损压缩：

[
L_{\mathrm{critical}}
=====================

L_{\mathrm{condition}}
+
L_{\mathrm{invalidation}}
+
L_{\mathrm{falsifier}}
+
L_{\mathrm{uncertainty}}
+
L_{\mathrm{provenance}}
+
L_{\mathrm{claim\ status}}
=0
]

即必须精确保留：

* 适用条件；
* 无效原因；
* 反证条件；
* 可推翻实验；
* 不确定度；
* 原始数据指针；
* 当前主张等级；
* 依赖的Skill版本；
* 人工干预。

可以压缩的主要是：

* 重复性自然语言描述；
* 冗余操作细节；
* 已由结构化事件完整表达的对话；
* 不影响后续决策的解释性措辞。

---

## 8. 证明携带型摘要

每个摘要不是一段孤立文字，而是：

[
M=
\left(
\text{summary},
\text{support sets},
\text{invalidation frontier},
\text{source hashes},
\text{compressor version},
\text{certificate}
\right)
]

其中证书证明：

1. 所有高等级主张仍有原始证据指针；
2. 所有反驳路径均被保留；
3. 所有关键失效节点均可传播；
4. 压缩前后预定义决策测试一致；
5. 压缩前后主张状态一致；
6. 压缩前后反事实撤销测试一致。

摘要只能作为缓存：

[
\text{summary}\neq\text{evidence}
]

Agent不得把摘要当成原始证据引用。

---

## 9. 计算复杂度与算法路线

近期率失真Agent记忆研究已经指出，最优预算记忆分配在一般情况下是NP完全问题，因此不能期待全局精确求解。([arXiv][4])

可采用以下近似：

[
V(e)=
\mathbb E[\Delta\operatorname{BayesRisk}\mid e]
+
\lambda
\mathbb E[\Delta\operatorname{FalsificationPower}\mid e]
+
\mu
\operatorname{CutImportance}(e)
]

其中：

* 第一项：证据对后续决策的价值；
* 第二项：证据对推翻错误结论的价值；
* 第三项：证据是否是主张图中的关键割点。

优先保留：

* 位于多个主张最小支持集中的证据；
* 可能引发大范围失效传播的证据；
* 决策边界附近的证据；
* 反例和失败记录；
* 不可替代的批次与状态信息。

---

## 10. 真正评价指标

* **反事实失效传播准确率**；
* **压缩前后主张等级一致率**；
* **压缩前后下一实验决策差异**；
* **冲突保留率**；
* **错误自我引用率**；
* **被作废证据重新检索率**；
* **独立支持链存活准确率**；
* **token成本—科学失真曲线**；
* **多Agent并发下的主张序列化错误率**；
* **科学记忆污染半衰期**。

---

# 三、可认证科学技能编译器与双账户认知博弈

## Certified Scientific Skill Compiler with Dual-Account Epistemic Governance

## 1. 根本问题

当前Agent Skill通常是：

* Markdown说明；
* 脚本；
* 模板；
* 示例；
* 工具描述。

但近期SkillsBench发现，人工整理的Skills平均可以改善任务表现，但84项任务中仍有16项出现负迁移；模型自行生成的Skills平均没有收益。SkillEvolBench进一步发现，当前Agent经常只能形成局部适应，难以形成在冻结部署、上下文变化和多技能组合下稳定复用的Skill，甚至直接保留原始轨迹比蒸馏后的Skill更有效。([arXiv][5])

所以真正的问题不是：

> Agent会不会生成Skill？

而是：

> 一个Skill凭什么获得控制真实实验的资格？

---

## 2. 把Skill定义为随机物理合同

定义科学Skill：

[
S=
\left(
\tau_{\mathrm{in}},
\tau_{\mathrm{out}},
\Phi_{\mathrm{pre}},
\Pi,
K,
\Psi_{\mathrm{post}},
\Omega,
\mathcal F,
\mathcal R,
\mathcal V
\right)
]

其中：

* (\tau_{\mathrm{in}},\tau_{\mathrm{out}})：强类型输入输出和单位；
* (\Phi_{\mathrm{pre}})：物理和信念状态前置条件；
* (\Pi)：执行策略；
* (K)：随机状态转移核；
* (\Psi_{\mathrm{post}})：后置条件；
* (\Omega)：副作用和不可逆性；
* (\mathcal F)：失效模式；
* (\mathcal R)：恢复策略；
* (\mathcal V)：验证器集合。

用概率Hoare三元组表达：

[
\left{
b\in\mathcal B_{\mathrm{pre}}
\right}
S
\left{
P
\left(
b'\in\mathcal B_{\mathrm{post}}
\land C_M=1
\right)
\ge1-\delta
\right}
]

注意前置条件不再是：

```text
temperature == 20
```

而是：

[
P
\left(
x\in\mathcal X_{\mathrm{ready}}
\mid D
\right)
\ge1-\epsilon
]

因为真实物理状态只能以置信形式表达。

---

## 3. Skill必须经过编译，而不是被Agent直接解释

自然语言Skill只是源代码，不能直接执行。

建议定义一个Scientific Skill IR：

```text
Skill Source
→ Typed Skill IR
→ Static Verification
→ State-Machine Compilation
→ Active Failure Testing
→ Certificate
→ Runtime Monitor
```

### 静态编译检查

* 单位和量纲；
* 参数范围；
* 仪器能力；
* 样品和设备锁；
* 前后置条件；
* 不可逆副作用；
* 危险动作；
* 依赖Skill版本；
* 允许的恢复路径；
* 哪些输出可以成为科学证据。

### 动态编译检查

* 仿真与数字孪生测试；
* shadow执行；
* 故障注入；
* 边界条件测试；
* 多Skill组合；
* 上下文变化；
* 不同仪器或固件版本；
* 真实小规模canary实验。

---

## 4. Skill组合必须有可证明的兼容性

两个Skill可以组合，当且仅当：

[
\Psi_i
\models_{\epsilon}
\Phi_{i+1}
]

即前一个Skill的后置信念状态，以至少 (1-\epsilon) 的概率满足后一个Skill的前置条件。

若每个Skill的条件失败风险为 (\delta_i)，则组合风险为：

[
\delta_{\mathrm{chain}}
\le
1-
\prod_{i=1}^{n}(1-\delta_i)
]

在小风险条件下：

[
\delta_{\mathrm{chain}}
\approx
\sum_{i=1}^{n}\delta_i
]

这样，“单个Skill测试通过”不再自动意味着“Skill组合安全”。

---

## 5. 核心原创：双账户认知博弈

普通多Agent系统让Agent同时：

* 提出方案；
* 预测结果；
* 执行动作；
* 评价自己；
* 决定是否成功；
* 更新记忆。

这相当于让一个参与者同时当运动员、裁判和记分员。

多Agent辩论也不能解决该问题。实证研究表明，Agent可能在辩论中从正确答案转向错误答案，偏向一致而不是抵抗错误推理；因此一致意见不是独立证据。([arXiv][6])

建议建立两个严格分离的账户。

---

### 账户A：认知账户 Epistemic Account

每个Agent或Skill在实验前必须提交概率预测：

[
p_{i,t}(y)
]

实验后使用严格适当评分规则：

[
w_{i,t+1}
=========

w_{i,t}
\exp
\left[
-\eta
\ell
\left(
p_{i,t},y_t
\right)
\right]
]

其中可以使用：

* log loss；
* Brier score；
* CRPS。

严格适当评分规则在相应假设下，使真实概率分布成为期望得分最优报告，因此适合作为Agent置信校准和长期信誉更新的数学基础。([统计网站][7])

认知账户衡量：

* 预测是否校准；
* 在什么适用域可靠；
* 是否经常过度自信；
* 对哪些失效模式判断差；
* 是否能够正确拒答。

---

### 账户B：行动账户 Execution Account

行动权限由独立控制器决定：

[
\operatorname{Act}(S,z)=1
\iff
\widehat R_S(z)
+
U_{\alpha}(z)
\le
R_{\max}
]

并且：

* Skill证书有效；
* 当前没有记忆冲突；
* 当前状态位于认证适用域；
* 没有检测到分布漂移；
* 物理前置条件满足；
* 风险预算未耗尽；
* 必要审批已获得。

最重要的机制设计原则是：

> **Agent不能因为获得自主执行权而直接获得认知奖励。**

否则Agent会有动机夸大置信度，以提高被选中执行的概率。

因此：

* 预测质量进入认知账户；
* 执行权限由外部风险清算器决定；
* 两者不能共享同一个奖励函数；
* Agent不能通过“更自信”直接获得更多权限。

这是比简单Brier评分更关键的博弈论设计。

---

## 6. 提议者—证伪者—执行者—审计者分权

建议四种角色：

| 角色  | 权限            | 禁止事项         |
| --- | ------------- | ------------ |
| 提议者 | 提出Skill、计划和预测 | 不得批准自己的Skill |
| 证伪者 | 搜索反例和边界失效     | 不得修改执行数据     |
| 执行者 | 执行已批准状态机      | 不得决定科学结论     |
| 审计者 | 检查证据、证书和主张    | 不得操作仪器       |

证伪者的收益不是“赢得辩论”，而是找到一个：

* 可重复；
* 可定位；
* 可归因；
* 能使Skill证书缩小适用域

的真实反例。

提议者只有在**过度自信**或违反合同条件时损失信誉，而不是在诚实报告高不确定性后实验失败时被惩罚。

这样能够避免系统把失败隐藏起来。

---

## 7. 用主动实验设计认证Skill，而不是优化材料

Skill认证本身应成为一个主动实验设计问题。

设上下文为 (z)，Skill失效概率为：

[
p_F(z\mid S,D)
]

下一测试条件选择：

[
z^*
===

\arg\max_z
\left[
p_F(z\mid S,D)
\cdot
\operatorname{Severity}(z)
+
\kappa I(\theta_S;Y_z\mid D)
----------------------------

\lambda C(z)
\right]
]

它故意寻找：

* Skill最可能失败的边界；
* 当前证书最不确定的区域；
* 多Skill组合最脆弱的位置；
* 分布漂移下的失效点；
* 记忆缺失、延迟或冲突下的异常行为；
* 仪器返回模糊状态时的恢复错误。

这相当于：

> **面向Skill失效边界的反向贝叶斯优化。**

目标不是找到性能最好点，而是尽可能高效地找到反例。

---

## 8. Skill证书不是一个成功率

Skill证书应定义为：

[
\mathcal C_S=
\left(
\mathcal D_S,
\hat R_S,
U_S,
\mathcal F_S,
\mathcal V_S,
h_S,
v_S,
t_S
\right)
]

其中：

* (\mathcal D_S)：已验证适用域；
* (\hat R_S)：风险估计；
* (U_S)：风险上界；
* (\mathcal F_S)：已知失效模式；
* (\mathcal V_S)：验证测试；
* (h_S)：Skill及依赖代码哈希；
* (v_S)：版本；
* (t_S)：证书有效期。

Skill修改后，旧证书不能自动继承：

[
S^{v+1}\neq S^v
\Rightarrow
\mathcal C_{S^{v+1}}
\not\Leftarrow
\mathcal C_{S^v}
]

可以迁移先验，但必须重新验证受影响部分。

---

## 9. 动态自治，而不是固定Level 3或Level 4

Skill的自主权定义为：

[
A_t(S,z)
========

f
\left(
\text{certificate},
\text{calibration},
\text{drift},
\text{severity},
\text{memory conflict},
\text{state uncertainty}
\right)
]

例如：

| 状态            | 自治级别          |
| ------------- | ------------- |
| 证书有效、状态明确、低风险 | 自动执行          |
| 新上下文但风险较低     | shadow或canary |
| 置信度失准、记忆冲突    | 仅提出建议         |
| 物理状态不明或测量失效   | 停止闭环          |
| 高风险不可逆操作      | 人工批准          |
| Skill版本变化     | 自动降级并重新认证     |

因此自治不是系统的一次性标签，而是一个随证据动态变化的权限函数。

---

## 10. 真正评价指标

* **Skill负迁移率**；
* **跨上下文调用正确率**；
* **多Skill组合失败率**；
* **证书外执行率**；
* **错误自主执行率**；
* **风险预测覆盖率**；
* **Agent置信校准误差**；
* **反例发现效率**；
* **证书缩域正确率**；
* **版本升级后的失效检测率**；
* **未经认证Skill进入控制面的次数**；
* **单位成本发现一个真实失效模式的效率**。

---

# 三个创新之间的关系

它们不是互相替代，而是一个完整的新科学Agent基础理论。

```text
物理信念状态Harness
        ↓
决定什么动作真正发生、什么测量有资格成为证据

反事实证明携带型记忆
        ↓
决定证据如何支持主张、失效如何传播、什么可以被压缩

可认证科学Skill与认知博弈
        ↓
决定哪些程序能力有资格复用、组合和自主执行
```

缺少任何一个都会发生系统性失败：

| 缺失部分  | 后果                                |
| ----- | --------------------------------- |
| 没有方案一 | 系统不知道物理动作是否真实完成，可能把软件成功当科学成功      |
| 没有方案二 | 系统会保留失效结论、压缩掉反例，并形成自我引用式记忆污染      |
| 没有方案三 | Agent会把局部经验错误蒸馏成Skill，并在新条件下放大负迁移 |

---

# 最值得主攻的创新组合

## 第一优先：物理信念状态Harness

这是所有真实湿实验Agent的底座。其最强创新不是“接仪器”，而是提出：

> **物理提交、计量提交、认知提交必须被严格分离。**

它能够形成一套新的湿实验Agent事务语义。

## 第二优先：反事实证明携带型记忆

这是你们最有机会形成独特理论贡献的方向。其核心科学问题非常清楚：

> **在给定记忆预算下，什么信息可以忘记，而不会改变当前决策、科学主张及未来证据撤销后的结论？**

这比一般的RAG、长期记忆和记忆压缩高一个理论层级。

## 第三优先：可认证Skill与双账户认知博弈

这是风险最高但潜在影响最大的方向。其核心突破是：

> **把Skill从“程序说明包”提升为“带物理适用域、概率保证、失效模式和动态权限的可认证科学合同”。**

同时用博弈论把“说得准”和“获得行动权”分开，防止Agent通过过度自信争取更多自主权。

---

# 最终研究定位

三个方向可以统一为：

> **面向部分可观测、不可逆、高成本真实实验的可信Scientific Agent基础理论：研究物理动作何时提交为事实、证据如何压缩而不失真，以及程序技能如何获得可撤销的自主执行资格。**

最核心的创新指标不再是材料性能，而是：

[
\boxed{
\text{错误认知提交率}
+
\text{反事实记忆失真}
+
\text{未经认证自主执行风险}
}
]

能够把这三个量同时定义、建模、约束并在真实湿实验中验证，本身就足以构成一个明显区别于现有自驱动实验室工作的研究体系。

[1]: https://www.nist.gov/programs-projects/development-standards-support-modular-and-autonomous-laboratory-ecosystem "Development of Standards to Support a Modular and Autonomous Laboratory Ecosystem | NIST"
[2]: https://arxiv.org/html/2602.22302v1 "Agent Behavioral Contracts: Formal Specification and Runtime Enforcement for Reliable Autonomous AI Agents"
[3]: https://www.w3.org/TR/prov-dm/ "PROV-DM: The PROV Data Model"
[4]: https://arxiv.org/html/2605.10870v1 "Remember the Decision, Not the Description: A Rate-Distortion Framework for Agent Memory"
[5]: https://arxiv.org/html/2602.12670v1 "SkillsBench: Benchmarking How Well Agent Skills Work Across Diverse Tasks"
[6]: https://arxiv.org/html/2509.05396v1 "Talk Isn’t Always Cheap: Understanding Failure Modes in Multi-Agent Debate"
[7]: https://sites.stat.washington.edu/raftery/Research/PDF/Gneiting2007jasa.pdf?utm_source=chatgpt.com "Strictly Proper Scoring Rules, Prediction, and Estimation"

问：附件是结合上面的创新点的分析，然后根据我现有的情况，做的一个比较具体的工作，请你结合之前的对话内容，客观分析一下，做出评价。

回答：
# 总体评价

基于两份附件，我的客观判断是：

> **这是一个相当扎实、诚实度很高的真实湿实验 Agent 项目，但目前最强的贡献是“证据治理型半闭环实验系统”，还不是前面讨论的三项底层创新——物理信念状态 Harness、证明携带型记忆、可认证 Scientific Skills——的完整实现。**

项目报告清楚区分了真实、冻结、代理、存根和负结果；系统架构也明确标出了哪些环节使用 LLM、哪些完全确定性、哪些不能进入主流程。这种透明度明显强于大量只展示一次成功轨迹的 Agent 项目。 

但以下评价仍是**文档与架构层面的审查**。附件没有包含源代码仓库、原始 EIS 数据、完整测试报告和运行日志，因此我不能独立确认所有定量结果是否可复算，也不能确认实际系统行为与架构文档完全一致。

---

# 一、我对项目的准确定位

目前最合适的名称是：

> **面向 EIS-only、部分可观测湿实验的证据治理型、人工参与的半闭环自主实验系统。**

不宜直接称为：

* 完整自驱动实验室；
* 具备长期科学记忆的多 Agent 系统；
* 具备可认证技能的 Scientific Agent；
* LLM 独立完成的材料发现系统；
* 已实现三重提交的物理科学 Harness。

原因是实际流程中，样品制备、campaign 选择和部分实验启动仍由人完成；LLM 主要参与上游文献推理以及对优化器建议的修正，S09 候选生成和 S10 排序则被明确冻结为确定性过程。

按照现有 SDL 评价框架，只有在实验执行、系统复位、数据采集分析和下一实验选择均不需要人工干预时，才属于严格的闭环自主系统；你们更接近“半闭环、人工参与的物理实验 + 自动分析和决策”类别。([Nature][1])

这不是贬低。对于昂贵、低吞吐、难以完全机器人化的实验，**可靠的半闭环可能比不可靠的全自动闭环更有科学价值**。

---

# 二、综合成熟度评价

以下评分中，5分表示“已经接入主流程、通过真实故障验证、能跨实验复现”。

| 维度                      |       成熟度 | 评价                                       |
| ----------------------- | --------: | ---------------------------------------- |
| 真实湿实验与数据流水线             | **3.5/5** | 已有真实 EIS、QC、分析、闭环接口和多批数据                 |
| 科学诚信与主张约束               |   **4/5** | 明确负结果、冻结项、代理项和 EIS-only 上限               |
| 复现与前瞻留痕                 |   **3/5** | 有 Git 冻结、SHA256、replay/real 分层，但复现对象仍不完整 |
| 线 A 迁移发现证据              |   **3/5** | 有多批正例和边界反例，但“可调”和机理证据不足                  |
| 线 B 闭环优化证据              |   **2/5** | 闭环真实，但只有10点、2个前瞻轮、无同配方重复                 |
| Agent 的不可替代贡献           | **1.5/5** | LLM 参与真实，但当前主要作用可被规则或约束优化替代              |
| 三重提交 Scientific Harness | **1.5/5** | 有雏形，没有完整物理提交和故障协调语义                      |
| 多 Agent 科学记忆            |   **1/5** | 有 trial DB、哈希、episodic memory，但无证据失效传播   |
| 记忆压缩                    | **0.5/5** | 附件没有显示真正的压缩算法和保真评测                       |
| 可认证 Scientific Skills   | **0.5/5** | 有类型、边界和函数模块，但尚无 Skill 合同与证书              |
| 跨体系、跨仪器一般化              |   **1/5** | 目前主要是单实验入口、少量材料体系的案例验证                   |

因此：

* **作为真实实验中的证据治理案例：中上水平。**
* **作为基础性 Scientific Agent/Harness 研究：仍处在原型早期。**
* **作为“多 Agent 记忆 + Skills”研究：目前证据明显不足。**

---

# 三、项目最强的地方

## 1. 最有价值的不是材料性能，而是三个“负能力”结果

你们现在最有思想价值的结果其实是：

1. **单曲线 QC 不能预测跨样品复现性**，AUROC 约0.52；
2. **校准可以消除过度自信，但不能增加分辨率**，ECE下降而 resolution 基本不变；
3. **EIS 在当前温区和数据质量下不能区分多个传导模型**。

这三个结果共同说明：

> **治理可以限制 Agent 乱说，但治理本身不能创造实验中不存在的信息。**

这是很强的 Scientific Agent 命题，甚至比“BO找到了一个更好配方”更重要。项目报告没有隐藏这些负结果，是明显优势。

你们真正捕捉到的是：

[
\text{可信度提升}\neq\text{可观测性提升}
]

以及：

[
\text{置信校准}\neq\text{知识分辨能力提升}
]

这与当前 SDL 领域的关键缺口高度一致：现有硬件经常无法完整捕获实验元数据，实验不确定度和校准尚未充分接入自主决策流程。([Nature][2])

---

## 2. 确定性与 LLM 权限分离做得正确

以下设计是好的：

* Stage0 分析、MOBO、终止、安全和主张等级主要由确定性代码控制；
* LLM失败可以降级到优化器；
* 实测无效时拒绝写入真实历史库；
* replay和real分离；
* S8母体系不进入凹凸棒土BO训练库；
* EIS-only硬封顶，不自动声称结构或唯一机理；
* 失效的DRT不作为证据。

这符合正确的 Scientific Agent 原则：

> **LLM负责开放式建议，确定性程序负责计算、边界和证据准入。**

---

## 3. 前瞻冻结和“不美化空结果”具有论文价值

官方recipe冻结、Git时间戳、三件套哈希链和“真实null”都是有效贡献。尤其是没有把闭环顶端候选差异硬说成显著提升，而是用复现地板解释“为什么当前数据无法支持改进”，这一点很好。

现有 SDL 文献建议不仅报告优化结果，还要报告自主度、实验精度、参数空间、运行寿命、吞吐、成本和优化表现。你们已经覆盖了其中一部分，但仍需补运行寿命、人工干预和失败恢复等指标。([Nature][1])

---

# 四、目前最关键的科学风险

## 1. Rb提取方法随温区切换，可能制造或放大所谓“转变”

这是我认为当前最需要优先排除的问题。

附件显示，系统在深冷区域优先使用低频平台法，而其他温区优先使用过零点、寻谷、平台或等效电路兜底。也就是说：

[
\hat R_b(T)=
\begin{cases}
f_{\mathrm{plateau}}(Z), & T\in\text{深冷区}\
f_{\mathrm{zero/valley/ECM}}(Z), & T\in\text{其他温区}
\end{cases}
]

而你们报告的转变温度恰好位于低温区附近。

因此存在一个必须排除的替代解释：

> 曲线斜率或断点的一部分，可能来自**电阻提取算法的切换**，而不是材料物理状态的改变。

这不等于当前转变是假的，但现有附件没有证明它不是算法路由造成的。

必须至少提供：

* 全温区使用同一种Rb方法的结果；
* 四种方法分别处理完整温度序列的敏感性分析；
* 将`rb_method`作为协变量的分层分析；
* 平滑单Arrhenius合成谱经过完整管线后的假阳性率；
* 在真实噪声和频率窗下的模拟电路恢复测试；
* 断点是否与算法切换温度重合的统计检验。

如果转变在所有固定提取策略下都存在，科学可信度会大幅提高。

---

## 2. KK失败只告警，不熔断，存在潜在证据泄漏

文档明确显示KK无效时只设置`kk_warning`，不会直接拒绝数据；Stage1只在`objective_valid=False`时阻止数据进入真实数据库。附件没有说明：

[
\text{KK失败}
\Rightarrow
\text{objective_valid=False}
]

因此目前无法确认疑似不满足线性、因果性或时间不变性的谱是否绝对不会进入BO。

Lin-KK的目的本来就是检验谱是否能被KK一致模型复现；但自动选择模型阶数、频率窗不足和过拟合都可能影响结果，因此不能只设置一个中位数阈值后完全依赖它。([KIT IAM][3])

建议把状态显式拆成：

```text
physical_operation_status
measurement_validity
parameter_extractability
scientific_admissibility
```

例如KK失败的数据可以用于故障诊断，但默认不得进入：

* BO训练集；
* 转变检测；
* 机理比较；
* Skill认证；
* 长期科学记忆。

除非有明确的降级规则和不确定度膨胀。

---

## 3. “复现地板”目前是一个混合方差，不是真正的物理地板

当前0.245–0.32 dex大概率混合了：

* 仪器重复误差；
* Rb拟合误差；
* 厚度和面积误差；
* 同片重复误差；
* 不同膜片误差；
* 批次误差；
* 日期和环境差异；
* 热史和装夹差异。

真正的复现地板应是一个方差分解：

[
\sigma^2_{\mathrm{total}}
=========================

\sigma^2_{\mathrm{instrument}}
+\sigma^2_{\mathrm{fit}}
+\sigma^2_{\mathrm{geometry}}
+\sigma^2_{\mathrm{specimen}}
+\sigma^2_{\mathrm{batch}}
+\sigma^2_{\mathrm{day}}
+\sigma^2_{\mathrm{interaction}}
]

否则“候选差异低于地板”只能说明低于一个混合经验尺度，不能指出限制究竟来自材料、制备还是测量。

更重要的是，线 B 当前使用的是线 A 的代理地板。不同材料、厚度、装夹和电阻范围下，误差结构可能完全不同。因此G1是必要的，但只在最佳配方处做三次重复，最多给出**局部重复性**，还不能给出整个搜索域的异方差噪声模型。

---

## 4. 当前“可辨识性天花板”只是经验不可区分，不是理论上界

Arrhenius、Mott和VTF的(\Delta R^2=0.006)可以支持：

> 在现有温区、采样点和噪声水平下，这几个模型表现近似。

但不能直接支持：

> EIS原则上无法区分这些模型。

真正的可辨识性天花板应是实验设计条件下的信息上界，例如：

[
\max_{a\in\mathcal A_{\mathrm{feasible}}}
I(H;Y_a\mid D)<\varepsilon
]

其中 (a) 包括允许的温度点、频率窗、重复次数、升降温路径和等待时间。

当前结果更准确的名称应该是：

> **设计条件下的实践不可辨识性**
> design-conditional practical non-identifiability

而不是一般性的理论天花板。

最新温度依赖传导分析工作也强调，需要同时进行参数不确定度、模型选择和外推不确定度分析，而不能只依赖拟合优度差异。([APS Journals][4])

---

# 五、线 B 闭环的核心问题

## 1. 没有重复，BO事实上不知道噪声有多大

10个trial全部是不同配方，意味着代理模型无法从数据中区分：

[
y=f(x)+\epsilon_{\mathrm{measurement}}
+\epsilon_{\mathrm{batch}}
+\epsilon_{\mathrm{preparation}}
]

究竟是性能表面变化，还是实验噪声。

这会直接影响：

* GP预测方差；
* EI和ParEGO采集函数；
* 是否认为已经收敛；
* 最优配方排序；
* 是否值得重复；
* “BO比random快”的可信度。

所以G1不仅是论文补数据，而是对整个BO观测模型的最低限度修复。

---

## 2. “BO 4.1 vs random 5.5”目前只能算回放证据

附件把replay和real分开，这是正确的。但这也意味着：

> BO优于随机的结果是对现有历史数据的回放比较，不是多次前瞻真实campaign中的性能优势。

要支持BO确实更有效，需要：

* 多随机种子；
* 相同初始点；
* 相同实验预算；
* 相同噪声处理；
* bootstrap置信区间；
* prospective hit-rate或simple regret；
* 最好具有独立campaign。

否则1.4次实验的平均差异很容易受初始点和历史数据分布影响。

---

## 3. LLM的安全价值尚未被证明不可替代

目前最突出的案例是：

[
R=0.0285\rightarrow R=0.28
]

但系统已经有确定性安全边界和SafetyValidator。因此需要比较：

1. 原始MOBO；
2. 约束MOBO；
3. 边界投影MOBO；
4. 规则修复；
5. LLM修复；
6. LLM加确定性验证。

如果确定性投影也会把配方拉回安全内部，那么当前证据只能说明：

> LLM完成了一次合理的安全修正。

不能说明：

> LLM给闭环带来了独特安全能力。

当前LLM角色更接近**有边界的策略顾问**，这其实是很合理的定位，但不要写成“LLM负责安全”。

---

## 4. 系统内部存在至少两套不同的标量分数

附件同时出现：

[
\text{combined score}
=====================

\log_{10}\sigma
-3E_{a,\mathrm{high}}
-0.5E_{a,\mathrm{excess}}
]

和：

[
\text{score}_{v3}
=================

\log_{10}\sigma
-1E_{a,\mathrm{high}}
-0.2E_{a,\mathrm{excess}}
]

即使后者只是审计或展示指标，也必须在schema和论文中明确隔离：

* 哪个用于训练；
* 哪个用于best trial；
* 哪个用于停止；
* 哪个用于论文图；
* 哪个用于复现地板比较。

否则很容易出现“优化一个目标、用另一个目标宣布成功”的评价漂移。

---

# 六、对三项底层创新的逐项评价

## 创新一：三重提交的物理信念状态 Harness

### 已经有的雏形

* QA拒绝；
* 真实模式坏数据不写库；
* 安全边界；
* replay/real隔离；
* 前瞻冻结；
* append-only检查；
* 人工审批绑定；
* HOLD偏置。

这些是很好的基础。

### 尚未实现的核心

现在的系统基本是：

```text
测量完成
→ Stage0分析
→ objective_valid
→ 写入数据库
```

还不是：

```text
物理提交
→ 计量提交
→ 认知提交
```

缺少的包括：

* 动作究竟执行、未执行还是部分执行；
* 通信超时后先核对还是直接重试；
* 样品热史、湿度史、装夹史的版本化状态；
* 校准和环境状态是否绑定到每次测量；
* 不确定物理状态如何进入信念分布；
* 数据作废后是否自动撤销BO和主张更新；
* 单样品单写者和仪器排他锁；
* 恢复、补偿和隔离语义。

最新LAP协议已经提出仪器排他预留、安全确认token、物理量类型、校准和不确定度等Agent—仪器语义。因此你们仅做哈希和审批绑定，不足以单独构成协议级创新；真正能够超越现有工作的地方，应是把**物理提交、测量有效性和科学主张提交连接起来**。([arXiv][5])

### 评价

**已有工程前置条件，但核心理论和运行时语义尚未实现。**

---

## 创新二：反事实证明携带型科学记忆

### 目前真正有的

* JSON trial history；
* 原子写入和去重；
* sample ID与input hash；
* Stage输出哈希；
* episodic memory旁路；
* EvidenceUnit；
* SHA256链。

### 目前没有的

* `supports/refutes/depends_on/invalidates`证据图；
* 主张的最小支持集；
* 证据被撤销后的级联降级；
* 多Agent并发版本控制；
* Proposed、Validated、Contested、Refuted等状态机；
* 记忆冲突保留；
* 记忆压缩；
* 压缩前后决策一致性；
* 反事实撤证测试；
* 证明携带型摘要。

W3C PROV可以表达实体、活动、Agent和派生关系，但需要在其上扩展科学主张、反证和失效传播语义。([W3C][6])

你们现在有两个非常好的真实测试案例：

1. 历史EIS符号bug被修正；
2. 旧ECE随机划分泄漏值被废弃。

真正的证明携带型记忆应该做到：

```text
旧证据作废
→ 所有依赖的参数自动失效
→ 相关模型重新计算
→ 主张自动降级
→ 曾经依赖该结论的实验决策被标记
```

附件只说明结果被人工更正，没有证明系统能够自动完成这种传播。

MemoryAgentBench把记忆能力拆成检索、测试时学习、长程理解和选择性遗忘；当前系统没有对这些能力进行正式评测。([OpenReview][7])

### 评价

**当前是有完整性校验的实验历史库，不是证明携带型科学记忆。**

---

## 创新三：可认证 Scientific Skills与双账户博弈

### 目前已有的前置条件

* Pydantic结构化输出；
* 参数类型和值域检查；
* 安全验证；
* 确定性分析模块；
* fallback；
* Git版本和哈希；
* 人工审批验证工具。

这些可以成为Skill编译器的基础。

### 尚未实现的核心

附件没有显示一等对象意义上的Skill：

```text
skill_id
version
applicability_domain
preconditions
postconditions
side_effects
measurement_requirements
failure_modes
recovery
validators
certificate
expiry
```

也没有：

* Skill注册表；
* Draft/Shadow/Canary/Validated生命周期；
* Skill组合兼容性；
* 分布外自动降级；
* 故障注入认证；
* Skill负迁移评价；
* Agent预测信誉账户；
* 执行权限账户；
* proper scoring rule；
* 动态自治等级。

SkillsBench的结果表明，人工整理的Skills总体有益，但部分任务会出现负迁移，而模型自生成Skills平均并没有稳定收益。因此“把流程写成Skill”本身不能证明可靠。([arXiv][8])

### 评价

**目前是模块化程序和工具，不是可认证科学技能系统。**

---

# 七、Agent与多Agent部分的真实评价

## 1. Agent不是当前系统不可替代的核心

根据架构：

* Stage0确定性；
* MOBO确定性；
* 安全边界确定性；
* S09生成确定性冻结；
* S10排序确定性冻结；
* S14主张审计确定性；
* agentic critic/memory旁路也是确定性词面规则。

真正的LLM主要在：

* 文献和假设整理；
* 策略建议修正；
* 上游机制与描述符推理。

因此系统在没有LLM的情况下仍然可以运行主要闭环。

这意味着当前论文不应证明：

> Agent独立完成发现。

而应证明：

> 在确定性科学内核约束下，LLM可以作为有限权限的证据整理和策略建议模块参与真实实验。

这是更准确、也更容易站住的结论。

---

## 2. 当前“多Agent记忆”实际上不存在

`critic.py + memory.py + heartbeat.py`的确定性旁路更接近：

> **主张lint工具 + episodic日志 +健康检查。**

它不具备多Agent记忆通常需要的：

* 私有与共享记忆；
* 并发写冲突；
* 角色权限；
* 版本一致性；
* 证据冲突；
* 失效广播；
* 选择性遗忘；
* 记忆压缩。

建议在论文中不要把这一部分称为“多Agent记忆创新”，否则Agent系统审稿人会很快指出名实不符。

---

## 3. “40个过度声称降到0”主要证明词面护栏有效

11条或25条禁词规则能够把绝对化词语降到零，这是有用的工程验证，但它不能证明：

* 主张与证据真的一致；
* 数值没有选取偏差；
* 文献引用忠实；
* 结论没有遗漏反例；
* C4主张有足够支持。

例如把“证明了某机理”改成“与某机理一致”，可能通过词面检查，但仍然可能没有足够数据。

因此需要区分：

[
\text{linguistic compliance}
\neq
\text{epistemic validity}
]

盲评4.3对1.0是进一步证据，但应报告：

* 评审者数量；
* 是否真正盲法；
* 评分一致性；
* 样本量；
* 是否使用未见过的干扰候选；
* 是否包含语义上错误但措辞保守的困难样本。

---

# 八、几个需要修正的内部表述

## 1. “亚零度转变 + 可调”

目前跨材料体系的陡峭度差异只能支持：

> 不同材料体系表现出不同的温度响应。

它不能直接支持：

> 同一体系中的转变可通过配方连续调控。

因此在完成同一母体系的配方梯度前，“可调”应降为：

* “体系依赖”；
* “表现出可调迹象”；
* “调控性待前瞻验证”。

---

## 2. `mechanism_discovery`命名不恰当

系统虽然把它压到C4并保留替代解释，但字段名仍叫`mechanism_discovery`。

建议改成：

```text
mechanism_consistency
mechanism_compatibility
mechanism_level_interpretation
```

否则字段名会在代码、JSON和图中持续制造比实际证据更强的暗示。

---

## 3. “真实闭环”应分开“实现”和“验证”

架构规定，closed-loop主张只有在`prospective_real`且至少3轮时才得到支持；但报告称当前只有2个前瞻轮。

因此按照系统自己的规则：

> 闭环代码已经实现，但闭环科学主张尚未达到自身支持门槛。

G1新增实验如果只是固定配方重复，不一定构成新的Agent决策轮。因此除了G1，还需要确认是否满足自己的“三轮前瞻真实闭环”标准。

---

## 4. “v2恒HOLD”证明安全默认值，不证明治理有效

一个永远HOLD的系统几乎不会产生假阳性，但也无法释放任何正确主张。

必须报告风险—覆盖曲线：

[
\text{Risk}(c)
\quad\text{versus}\quad
\text{Coverage}(c)
]

包括：

* 正确放行率；
* 错误放行率；
* 不必要HOLD率；
* 人工审批负担；
* 不同主张等级的选择性风险。

否则v2只能被称为**fail-closed守卫原型**。

---

# 九、发表就绪度的客观判断

我不认同“唯一硬门槛只有G1”这个判断。

## 若论文定位为材料/SDL案例研究

题目类似：

> **Evidence-governed semi-closed-loop discovery under EIS-only and low-data constraints**

那么当前已经接近可投稿状态，但至少还有以下硬门槛：

1. G1线B同配方直接重复；
2. Rb提取方法切换敏感性；
3. 平滑无转变数据的全流程假阳性测试；
4. 断点和Ea的bootstrap或层级贝叶斯区间；
5. KK warning到`objective_valid`的明确准入测试；
6. LLM修正与确定性约束修正的对照；
7. 完整报告人工干预率、运行时长、失败次数和实验成本。

这种定位下，中等水平专业期刊是现实目标，但具体期刊档次不能仅凭两份内部报告确定。

## 若论文定位为Scientific Agent基础研究

题目涉及：

* Tri-Commit Harness；
* proof-carrying memory；
* multi-agent memory；
* memory compression；
* certified skills；
* dynamic autonomy。

那么当前明显还没有到投稿线。至少需要三类真实演示：

### 演示A：物理提交故障注入

人为制造：

* 仪器超时；
* 文件已生成但确认消息丢失；
* 温度未平衡；
* 校准过期；
* 样品状态不明。

证明系统不会盲目重试，也不会让无效数据进入BO。

### 演示B：证据失效传播

撤销历史KK符号bug相关结果或旧ECE泄漏结果，证明：

* 下游参数自动作废；
* 模型和排序自动重算；
* 主张自动降级；
* 摘要和Agent上下文不再检索失效结论。

### 演示C：Scientific Skill认证

选择一个真实EIS Skill，完成：

* 强类型合同；
* 前后置条件；
* 适用域；
* 故障注入；
* shadow/canary；
* 版本升级；
* 负迁移测试；
* 证书外自动拒绝。

没有这三类证据，前述三项创新只能写成**理论框架或未来工作**，不能写成已经完成的核心贡献。

---

# 十、最适合你们当前项目的主线

现有结果中最强、最统一的科学命题不是：

> Agent发现了某种材料。

而是：

> **在只有EIS、样本少、实验昂贵、结构不可见的真实湿实验中，校准和治理能够降低过度声称，却无法突破由复现噪声和观测不可辨识性共同形成的认知边界。**

可以表达为：

[
\boxed{
\text{可信自治上限}
=============

\min
\left(
\text{复现能力},
\text{可辨识能力},
\text{执行可靠性},
\text{证据治理能力}
\right)
}
]

其中：

* 复现地板决定“多小的提升无法相信”；
* 可辨识性限制决定“多强的机制主张无法支持”；
* Harness决定“哪些测量有资格成为证据”；
* 校准决定“Agent应该以多大置信度说话”。

这是当前项目真正具有原创潜力的地方。

---

# 最终结论

**项目不是空壳，也不是简单把LLM接到BO上。**真实数据、负结果、冻结纪律、确定性内核和主张约束都做得相当认真。

但必须把三件事分清：

1. **你们已经很好地实现了证据留痕和保守护栏；**
2. **你们尚未实现物理状态级Scientific Harness；**
3. **你们尚未实现证明携带型记忆、记忆压缩和可认证Skills。**

因此最客观的评价是：

> 当前项目已经足以支持一篇有价值的“表征受限条件下可信半闭环实验”案例论文；但若声称完成了前述三项Scientific Agent底层创新，则会明显过度。G1是必要实验补丁，却不是唯一技术门槛。真正决定项目能否上升为基础性Agent研究的，是能否把物理提交、证据失效传播和Skill认证从旁路概念真正接入主流程。

[1]: https://www.nature.com/articles/s41467-024-45569-5 "Performance metrics to unleash the power of self-driving labs in chemistry and materials science | Nature Communications"
[2]: https://www.nature.com/articles/s41467-025-59231-1 "Science acceleration and accessibility with self-driving labs | Nature Communications"
[3]: https://www.iam.kit.edu/et/english/Lin-KK.php?utm_source=chatgpt.com "Kramers-Kronig Validity Test Lin-KK for Impedance Spectra"
[4]: https://journals.aps.org/prxenergy/abstract/10.1103/nxgq-lmp6?utm_source=chatgpt.com "Bayesian Methods for the Investigation of Temperature ..."
[5]: https://arxiv.org/html/2606.03755v1 "LAP: An Agent-to-Instrument Protocol for Autonomous Science"
[6]: https://www.w3.org/TR/prov-dm/ "PROV-DM: The PROV Data Model"
[7]: https://openreview.net/forum?id=DT7JyQC3MR "Evaluating Memory in LLM Agents via Incremental Multi-Turn Interactions | OpenReview"
[8]: https://arxiv.org/abs/2602.12670?utm_source=chatgpt.com "SkillsBench: Benchmarking How Well Agent Skills Work Across Diverse Tasks"

问：
请你针对出现的问题，提供详细的优化方案，除了G1新增实验和后续的凹凸棒土闭环实验，其他的实验不再增加，只在代码层面上进行修改。

回答：
# 总体优化方向

在“不再增加其他湿实验”的约束下，最合理的路线不是继续给现有系统堆模块，而是把已有数据、历史故障、冻结配方和未来允许执行的G1/凹凸棒土闭环，转化为四类代码资产：

1. **历史数据重分析资产**：排除Rb方法切换、单调性过滤和模型选择带来的算法伪影；
2. **软件仿真资产**：合成无转变谱、注入已知转变、故障注入和时间顺序回放；
3. **科学运行时资产**：三重提交Harness、证据准入、失效传播；
4. **可验证Agent资产**：结构化记忆、压缩证书、Scientific Skills合同。

当前代码已经具备真实Stage0、Stage1、前瞻冻结、哈希链、坏数据拒绝和主张封顶，但KK仍是告警模式，Rb在深冷和其他温区采用不同优先路由，Arrhenius序列还有单调性过滤；线B只有10个不同配方、没有重复，LLM主要在Step 5修正优化器建议。 

建议把系统升级为：

```text
原始EIS
  ↓
所有Rb方法并行估计 + 不确定度
  ↓
物理状态 / 测量有效性 / 参数可提取性
  ↓
科学证据准入
  ├── 诊断库
  ├── BO训练库
  └── 主张证据图
  ↓
噪声感知MOBO + 受限LLM顾问
  ↓
三重提交Harness
  ↓
证明携带型记忆 + 可认证Scientific Skills
```

NIST目前也把自主实验室底层标准拆成样品管理、仪器通信、数据知识管理和算法模型集成四个方面；你们可以直接围绕这四层重构，而不需要扩展材料体系。([NIST][1])

---

# 一、先冻结旧系统，不要直接改写既有结果

这是所有后续工作的第一步。

## 1. 保留legacy输出

现有以下产物全部只读：

* `aggregated_results.json`
* `arrhenius_analysis.json`
* `history_db_attapulgite.json`
* `official_recipe.json`
* 已发表图表使用的历史分析结果
* 前瞻冻结commit及其SHA256链

新增版本化产物：

```text
aggregated_results_v2.json
arrhenius_analysis_v2.json
stage0_result_bundle_v2.json
history_db_attapulgite_v2.json
scientific_commit_ledger.jsonl
evidence_graph.sqlite
skill_registry.json
```

## 2. 所有分析代码显式携带版本

```python
analysis_version = "stage0-eis-v2.0"
admission_policy_version = "evidence-policy-v2.0"
objective_definition_id = "attapulgite-objectives-v2"
skill_version = "eis-point-analysis@2.0.0"
```

历史结果不覆盖，只生成：

```text
legacy_result
v2_result
delta_report
reason_for_delta
```

## 3. 配置先冻结，再跑最终重分析

新增：

```text
configs/
  stage0_v2_policy.yaml
  rb_method_policy.yaml
  claim_policy_v2.yaml
  objective_registry.yaml
  skill_certification_policy.yaml
```

阈值确定后先Git提交，再运行全部历史数据。否则新的稳健性分析也会面临“看完结果后调阈值”的质疑。

---

# 二、最高优先级：消除Rb方法切换造成的潜在转变伪影

当前最严重的代码风险是：

```text
深冷区：low_freq_plateau优先
其他温区：zero_crossing → valley → plateau → ECM
```

而你们关注的正是低温转变。

这并不证明转变是假的，但必须证明：

> 转变不是由分析算法随温度切换产生的。

## 1. 不再只输出一个Rb

修改：

```text
stage0_measurement/rb_fitting.py
```

保留原来的：

```python
fit_rb_and_conductivity(...)
```

新增：

```python
fit_all_rb_methods(...)
```

每个温点统一尝试四种方法：

```python
class RbEstimate(BaseModel):
    method: Literal[
        "reverse_zero_crossing",
        "reverse_valley",
        "low_freq_plateau",
        "equivalent_circuit",
    ]
    applicable: bool
    rb_ohm: float | None
    log10_rb_sd: float | None
    fit_score: float | None
    condition_number: float | None
    bootstrap_success_rate: float
    failure_reason: str | None
```

某种方法在某张谱上不适用时，不强行给数，而是：

```json
{
  "method": "reverse_zero_crossing",
  "applicable": false,
  "failure_reason": "no_valid_crossing_in_frequency_window"
}
```

## 2. 方法选择不能使用跨温度平滑性

不能用“哪种方法产生的Arrhenius曲线更平滑”作为方法选择标准，因为这会把真实转变也当成异常。

方法权重只能由单张谱内部信息决定：

* 频率窗覆盖；
* 平台斜率；
* 过零点附近局部稳定性；
* 拟合残差；
* 参数条件数；
* 频率子采样bootstrap稳定性；
* 是否触及边界；
* 是否依赖极少数频点。

建议在对数域计算稳健加权估计：

[
\hat r_t=
\operatorname{weighted\ median}
\left{
\log_{10}R_{b,t}^{(m)}
\right}
]

并输出方法间方差：

[
s_{\mathrm{method},t}^{2}
=========================

\operatorname{Var}*{m}
\left(
\log*{10}R_{b,t}^{(m)}
\right)
]

## 3. 每个样品至少产生四条分析轨迹

```text
legacy_routed
fixed_plateau
fixed_best_global_method
method_ensemble
```

其中`fixed_best_global_method`只能根据整个样品中各方法的：

* 可用覆盖率；
* 单谱bootstrap稳定性；
* 单谱拟合质量

选择，不能看断点位置和Ea结果。

## 4. 转变主张增加“方法不变性”门槛

新增：

```python
class TransitionRobustness:
    p_segmented: float
    breakpoint_median_K: float | None
    breakpoint_ci95_K: tuple[float, float] | None
    supported_methods: list[str]
    method_agreement_fraction: float
    leave_one_temperature_out_stability: float
    null_pipeline_false_positive_rate: float
    routing_artifact_risk: Literal["low", "medium", "high"]
```

主分析不能再只依据legacy routed结果。

建议只有满足以下条件才允许输出“稳定的经验转变”：

```text
多种固定Rb策略均支持分段
+ 断点区间高度重叠
+ 删除任一温点后不完全消失
+ 无转变合成数据的假阳性率低
+ 断点不是集中出现在路由切换区
```

如果不满足，降级为：

> “观测到低温曲率变化，但其幅度对Rb提取策略敏感。”

---

# 三、用合成数据测试整个Stage0，而不是只测试拟合函数

这部分不增加湿实验，也不能被当作材料证据，但能验证算法是否自己制造“发现”。

新增目录：

```text
stage0_measurement/synthetic_validation/
  smooth_null_generator.py
  injected_break_generator.py
  spectrum_residual_bootstrap.py
  full_pipeline_benchmark.py
```

## 1. 无转变合成谱

对每个真实数据集：

1. 使用单段平滑Arrhenius拟合目标 (R_b(T))；
2. 保留真实温度点和频率网格；
3. 选取真实谱形作为模板；
4. 按目标 (R_b(T)) 对谱进行缩放；
5. 从真实残差中重采样噪声；
6. 完整运行：

```text
QA → KK → 四种Rb方法 → σ → Arrhenius → 断点检测
```

计算：

[
\operatorname{FPR}_{\mathrm{break}}
===================================

P(\text{算法在无转变条件下报告转变})
]

重点观察：

* 假断点是否集中在深冷路由切换温区；
* 三段模型是否系统性过拟合；
* 单调性过滤是否放大断点；
* 某种Rb方法是否特别容易产生假转变。

## 2. 注入已知转变

在相同温度网格中注入不同强度、不同位置的已知断点，估计：

* 断点检出率；
* 断点位置偏差；
* 置信区间覆盖率；
* 最低可检出Ea差异；
* 不同温度采样密度下的检出能力。

最终输出：

```text
pipeline_false_positive_rate
pipeline_detection_power
breakpoint_bias_K
breakpoint_interval_coverage
minimum_detectable_slope_change
```

这会把“我们检测到转变”升级为：

> “在当前温度网格、噪声和分析流程下，该转变超过了管线的经验检出极限。”

---

# 四、重构KK、QA和科学证据准入

EIS解释通常要求系统满足线性、因果性和时间不变性，KK检验是评估一致性的重要工具；但有限频率窗也可能影响KK结果，所以不应简单做成“所有警告全部删除”，而应按用途分级。([PMC][2])

## 1. 不再使用单一`status`

新增：

```python
class EvidenceStatus(BaseModel):
    qa_status: Literal["PASS", "FAIL"]
    kk_status: Literal["PASS", "WARN", "FAIL", "NOT_TESTABLE"]
    rb_extractability: Literal["STRONG", "WEAK", "FAIL"]
    uncertainty_status: Literal["QUANTIFIED", "PARTIAL", "UNKNOWN"]
    scientific_admissibility: Literal[
        "PRIMARY",
        "SENSITIVITY_ONLY",
        "DIAGNOSTIC_ONLY",
        "REJECTED",
    ]
    reason_codes: list[str]
```

## 2. 分用途定义准入规则

新增：

```text
stage0_measurement/evidence_admission.py
configs/evidence_admission_v2.yaml
```

建议规则：

| 数据状态        | 诊断分析 | Arrhenius主分析 |   BO训练 |    科学主张 |
| ----------- | ---: | -----------: | -----: | ------: |
| QA失败        |  可保存 |           不可 |     不可 |      不可 |
| KK失败        |  可保存 |           不可 |     不可 |      不可 |
| KK警告、Rb一致   |    可 |        可做敏感性 | 可带方差膨胀 | 不作为唯一证据 |
| KK通过、Rb强可提取 |    可 |            可 |      可 |       可 |
| Rb方法严重不一致   |    可 |          敏感性 |     不可 |      不可 |

示例：

```python
def assess_admissibility(point: PointAnalysisV2) -> EvidenceStatus:
    if point.qa.failed:
        return rejected("QA_FATAL")

    if point.kk.status == "FAIL":
        return diagnostic_only("KK_INCONSISTENT")

    if point.rb.method_spread_dex > policy.max_method_spread:
        return sensitivity_only("RB_METHOD_DISAGREEMENT")

    if point.kk.status == "WARN":
        return sensitivity_only(
            "KK_WARNING",
            variance_multiplier=policy.kk_warn_variance_multiplier,
        )

    return primary()
```

## 3. `objective_valid`必须由准入策略生成

当前Stage1只检查`objective_valid`。以后：

```python
objective_valid = (
    scientific_admissibility == "PRIMARY"
    and objective_uncertainty_is_finite
)
```

禁止Stage1自行推断有效性。

## 4. 允许保存坏数据，但分库

```text
diagnostic_observations.jsonl
scientific_observations.jsonl
```

坏数据不能删除，因为它们对故障学习和Skill失效分析有价值；但不能进入BO、性能排序和主张图。

## 5. 必须增加的集成测试

```text
test_kk_fail_cannot_enter_real_history
test_qa_fail_cannot_enter_bo
test_kk_warn_inflates_variance
test_diagnostic_data_remains_queryable
test_objective_valid_is_policy_derived
```

目标是：

[
P(\text{无效谱进入BO})=0
]

---

# 五、取消“基于预期趋势删除点”的单调性过滤

当前`extract_valid_arrhenius_series()`会根据Rb单调性过滤反常跳点。

这在研究转变时具有明显风险：

> 真正的异常行为和实验伪影都可能表现为“不单调”。

## 修改原则

不得仅因为点“不符合预期温度趋势”就删除。

修改：

```text
stage0_measurement/arrhenius.py
```

将：

```python
filter_non_monotonic_points(...)
```

改为：

```python
flag_sequence_anomalies(...)
```

每个异常点保留：

```json
{
  "included_in_primary_fit": true,
  "sequence_anomaly": true,
  "anomaly_reason": "unexpected_local_jump",
  "raw_qc_valid": true
}
```

主拟合使用稳健误差模型，例如Student-t残差，而不是删除：

[
y_i \sim t_\nu(\mu_i,s_i)
]

同时输出三种敏感性：

```text
all_admissible_points
robust_regression
leave_one_point_out
```

只有原始谱存在可证明的QA问题，才能排除该点。

---

# 六、把单值电导率改为带不确定度的观测

当前Stage0主要输出点值。建议所有后续模块统一消费分布。

## 1. 每个温点输出

```python
class ConductivityEstimate(BaseModel):
    log10_sigma_mean: float
    log10_sigma_sd: float
    ci95: tuple[float, float]
    variance_components: dict[str, float | None]
    method: str
    uncertainty_scope: Literal[
        "RB_ONLY",
        "RB_AND_METHOD",
        "TOTAL_KNOWN",
        "PARTIAL",
    ]
```

## 2. 误差传播

[
\log\sigma
==========

\log t-\log R_b-\log A
]

近似为：

[
\operatorname{Var}(\log\sigma)
==============================

\operatorname{Var}(\log t)
+
\operatorname{Var}(\log R_b)
+
\operatorname{Var}(\log A)
]

若厚度或面积不确定度未知，不能默认为0，应标记：

```text
geometry_uncertainty_status = UNKNOWN
```

此时可以报告Rb和方法引入的不确定度，但不得声称获得了完整绝对电导不确定度。

## 3. `combined_score`也必须是分布

通过Monte Carlo传播：

```python
samples = (
    log_sigma_samples
    - 3.0 * ea_high_samples
    - 0.5 * ea_excess_samples
)
```

输出：

```text
combined_score_mean
combined_score_sd
combined_score_ci95
```

而不是仅保存一个确定值。

---

# 七、把断点判断从“最佳AICc模型”升级为不确定度判断

保留现有1段、2段、3段和非连续模型，但不再仅输出获胜模型。

## 新输出

```python
class SegmentedModelResult(BaseModel):
    model_probabilities: dict[str, float]
    p_any_break: float
    breakpoint_samples_K: list[float]
    breakpoint_ci95_K: tuple[float, float] | None
    slope_change_samples: list[float]
    leave_one_temperature_out_support: float
    method_sensitivity: dict[str, float]
    practical_identifiability: str
```

## 计算方式

使用谱级bootstrap：

```text
重采样原始频率残差
→ 重新做Rb提取
→ 抽样厚度/面积已知误差
→ 重新拟合分段模型
→ 保存模型和断点
```

而不是只对最终电导点做普通bootstrap。

## 转变主张建议分级

| 状态                | 允许表述           |
| ----------------- | -------------- |
| (P_\text{break})低 | 无证据支持稳定断点      |
| 概率高但方法敏感          | 观察到方法依赖的低温曲率变化 |
| 概率高、方法稳定          | 观察到稳健的经验传导区间转变 |
| 多模型都可解释           | 不声称唯一传导律或微观机制  |

---

# 八、把“可辨识性天花板”改为形式化的实践不可辨识性

当前的(\Delta R^2=0.006)是重要证据，但只说明现有拟合结果接近。

建议新增：

```text
_new_data_analysis/identifiability_v2/
  predictive_model_comparison.py
  model_confusion_simulation.py
  posterior_diagnostics.py
  design_conditional_ceiling.py
```

## 1. 使用预测能力而不是仅用拟合优度

对Arrhenius、VTF、Mott分别做：

* 留一温点预测；
* 预测对数得分；
* bootstrap差异区间；
* 参数后验或参数置信区间；
* 后验预测检查。

贝叶斯EIS模型研究已经表明，有些频率范围和噪声条件下，数据不足以统计支持某种ECM结构；后验参数和后验预测检查比“哪个拟合值最高”更能暴露这种不可辨识性。([Nature][3])

## 2. 构建模型混淆矩阵

分别从三个模型生成合成数据，使用真实：

* 温度网格；
* 数据点数；
* 实测噪声；
* 缺失点模式。

再由现有模型选择器判别。

得到：

| 真模型       | 判Arrhenius | 判VTF | 判Mott |
| --------- | ---------: | ---: | ----: |
| Arrhenius |          … |    … |     … |
| VTF       |          … |    … |     … |
| Mott      |          … |    … |     … |

若三者经常互相误判，则输出：

```text
design_conditional_practical_non_identifiability = true
```

准确表述应是：

> 在现有温区、采样点和噪声下，这些经验传导律实践上不可辨识。

而不是：

> EIS原则上永远无法区分这些机理。

---

# 九、重构复现地板：从一个数字变成有适用域的方差模型

现有0.245–0.32 dex混合了多个误差来源。建议拆成：

```python
class ReproducibilityFloor(BaseModel):
    metric_id: str
    scope: Literal[
        "ANALYSIS_ONLY",
        "WITHIN_SPECIMEN",
        "BETWEEN_SPECIMEN",
        "CROSS_BATCH",
        "LINE_B_LOCAL_DIRECT",
        "CROSS_SYSTEM_PROXY",
    ]
    sd_dex: float
    ci95: tuple[float, float]
    n: int
    source_datasets: list[str]
    applicable_materials: list[str]
```

## 1. 用现有数据估计层级方差

对已有LRS重复使用：

[
y_i
\sim
t_\nu
\left(
\mu,\sqrt{s_i^2+\tau^2}
\right)
]

其中：

* (s_i^2)：Stage0 bootstrap得到的分析不确定度；
* (\tau^2)：独立膜片间差异。

再分别做：

* 4月与6月分层；
* 0.02 cm与0.07 cm敏感性；
* 全数据结果；
* 六月标准化批次结果。

样本不多，不宜同时拟合大量随机效应；应输出宽区间，而不是一个看似精确的分解。

## 2. G1只负责一件事

G1完成后新增：

```text
LINE_B_LOCAL_DIRECT
```

它只能表示：

> 在R=0.186、N=1.029附近的局部重复性。

不能直接当作整个R/N搜索空间的全局异方差模型。

## 3. 不再用“差异是否小于一个地板”做二值判断

改成：

[
P\left(
\Delta>\delta_{\mathrm{meaningful}}
\mid D
\right)
]

例如：

```text
P(candidate improves best by > practical margin)
P(candidate is indistinguishable from best)
P(candidate is actually worse)
```

这样“诚实null”会成为概率结论，而不是硬阈值结论。

---

# 十、统一Stage1中的目标定义，消除评分漂移

当前存在：

[
\text{combined score}
=====================

\log_{10}\sigma-3E_a-0.5E_{a,\mathrm{excess}}
]

以及另一个：

[
\text{score}_{v3}
=================

\log_{10}\sigma-E_a-0.2E_{a,\mathrm{excess}}
]

即使一个用于训练、一个用于审计，也必须代码隔离。

新增：

```text
stage1_optimization/objectives/
  registry.py
  definitions.yaml
```

定义：

```yaml
objectives:
  - id: attapulgite_combined_score_v1
    role: optimization_primary
    expression: log10_sigma_room - 3.0*ea_high - 0.5*ea_low_excess
    direction: maximize
    units: dimensionless
    frozen: true

  - id: audit_score_v3
    role: diagnostic_only
    expression: log10_sigma_room - 1.0*ea_high - 0.2*ea_low_excess
    direction: maximize
    frozen: true
```

每条trial必须带：

```json
{
  "objective_definition_id": "attapulgite_combined_score_v1",
  "objective_definition_sha256": "...",
  "objective_role": "optimization_primary"
}
```

禁止：

```python
get_best_trial("combined_score")
```

改为：

```python
get_best_trial(objective_definition_id="attapulgite_combined_score_v1")
```

若历史DB中的目标哈希与campaign不一致，直接拒绝启动。

---

# 十一、把现有MOBO升级为噪声感知MOBO

## 1. 每个观测携带方差

```python
class ObjectiveObservation(BaseModel):
    mean: float
    variance: float
    variance_components: dict[str, float]
    noise_source: Literal[
        "ANALYSIS_PROPAGATED",
        "LINE_B_G1_DIRECT",
        "CROSS_SYSTEM_PROXY",
        "INFERRED",
    ]
```

建议：

[
Y_{\mathrm{var}}
================

s_{\mathrm{analysis}}^2
+
\tau_{\mathrm{lineB}}^2
]

G1前可以使用代理值做shadow或敏感性分析，但必须标记为`CROSS_SYSTEM_PROXY`；G1后使用凹凸棒土局部直接噪声。

## 2. 推荐迁移到BoTorch，但采用特性开关

当前BoTorch的`SingleTaskGP`支持传入已知观测噪声`train_Yvar`；若未提供，则推断同方差噪声。官方文档也明确指出，完整的可外推异方差噪声模型并非开箱即用，因此你们不应声称已经获得全空间异方差模型。([BoTorch][4])

新增：

```text
stage1_optimization/optimizers/botorch_mobo_v2.py
```

配置：

```yaml
optimizer_backend: legacy | botorch_v2
```

建议采用：

* 三个独立`SingleTaskGP`或`ModelListGP`；
* `train_Yvar`输入已知观测方差；
* 输入归一化；
* 输出标准化；
* 噪声多目标采集函数；
* 硬约束与软风险分离。

对当前三目标、带噪声问题，推荐使用`qLogNoisyExpectedHypervolumeImprovement`，而不是普通EI。当前BoTorch文档对带约束多目标问题提供qNEHVI/qParEGO实现，并建议使用log版本以改善数值问题。([BoTorch][5])

## 3. 不要把失败实验赋成低性能

建立两个模型：

```text
performance_model
measurement_validity_model
```

如果历史中有失败/无效记录：

[
P_{\mathrm{valid}}(x)
]

作为采集约束：

[
\alpha_{\mathrm{final}}(x)
==========================

\alpha_{\mathrm{performance}}(x)
\cdot
P_{\mathrm{valid}}(x)
]

数据少时用正则化逻辑回归或Beta-Binomial即可，不必强行上分类GP。

## 4. 未来闭环加入“重复还是新配方”的决策

不额外增加实验总数，只让后续允许的闭环轮次在两类动作中选择：

```text
NEW_FORMULATION
REPLICATE_EXISTING
```

重复动作的价值来自：

* 降低top候选排序不确定度；
* 判断提升是否超过地板；
* 校准噪声；
* 解决两个候选的排序冲突。

选择效用：

[
U(a)=
\mathbb E[\Delta HV]
+
\lambda I(\theta;Y_a)
---------------------

## \beta C(a)

\gamma R(a)
]

当认知不确定性占主导时选新配方；当测量噪声导致排序不稳时选重复。

---

# 十二、重写终止条件

当前终止条件包含最近5轮提升、最近3次位移和GP方差等规则。

保留为诊断，但新增概率终止条件：

[
P\left(
\max_x \Delta HV(x)>
\delta_{\mathrm{meaningful}}
\mid D
\right)<\alpha
]

以及：

[
P(\text{current best is truly best}\mid D)>1-\alpha
]

建议输出：

```python
class TerminationAssessment:
    budget_stop: bool
    safety_stop: bool
    posterior_improvement_probability: float
    ranking_stability_probability: float
    reproducibility_evidence_sufficient: bool
    verdict: Literal[
        "CONTINUE",
        "REPLICATE",
        "CAN_STOP",
        "MUST_STOP",
    ]
```

没有直接复现证据时，不能仅凭GP预测方差小就声称收敛。

---

# 十三、重新定位LLM：只能在Top-K中决策，不能自由修改配方

当前LLM把raw MOBO的R=0.0285修正为R=0.28，这说明它提供了合理的风险回避，但尚未证明这种价值无法被确定性规则替代。

## 1. 增加四个代码基线

```text
RAW_MOBO
HARD_CONSTRAINED_MOBO
DETERMINISTIC_PROJECTION
RULE_BASED_RISK_REPAIR
LLM_TOPK_SELECTION
```

在历史建议和合成边界建议上比较：

* 安全违规率；
* 与边界距离；
* 采集函数损失；
* 推荐多样性；
* 重复运行一致性；
* 失败降级率。

## 2. 推荐的LLM权限

优化器生成Top-K可行候选：

```python
candidates = optimizer.top_k_feasible(k=10)
```

LLM只能：

* 从Top-K中选择；
* 建议`HOLD`；
* 在有限trust region内提出delta；
* 引用具体风险规则和证据ID。

不能：

* 修改目标函数；
* 越过硬边界；
* 任意生成搜索域外配方；
* 把自己认为安全的候选绕过验证器；
* 直接写入真实history。

示例输出：

```json
{
  "action": "SELECT_TOP_K",
  "selected_candidate_id": "cand_04",
  "delta": null,
  "reason_codes": [
    "AVOID_LOW_R_BOUNDARY",
    "PRESERVE_EXPECTED_HYPERVOLUME"
  ],
  "evidence_ids": ["risk_rule:R_low_v2"],
  "confidence": 0.71
}
```

## 3. Prompt溯源不能只存最终prompt哈希

建议新增：

```python
class PromptEnvelope:
    template_git_commit: str
    system_template_sha256: str
    user_template_sha256: str
    input_artifact_hashes: list[str]
    rendered_prompt_sha256: str
    model: str
    temperature: float
    response_sha256: str
    retry_count: int
```

这样无需公开敏感prompt原文，也能重建其来源和输入。

---

# 十四、实现真正的三重提交Scientific Harness

这部分不需要额外湿实验，可以先以shadow模式接入G1，再用于后续凹凸棒土闭环。

## 1. 新增三个提交状态

```python
class PhysicalCommit:
    status: Literal[
        "NOT_STARTED",
        "DISPATCHED",
        "ACKNOWLEDGED",
        "EFFECT_OBSERVED",
        "PARTIAL",
        "UNKNOWN",
        "FAILED",
        "RECONSTRUCTED",
    ]

class MetrologicalCommit:
    status: Literal[
        "VALID",
        "CONDITIONAL",
        "INVALID",
    ]

class EpistemicCommit:
    status: Literal[
        "PRIMARY_EVIDENCE",
        "SENSITIVITY_ONLY",
        "DIAGNOSTIC_ONLY",
        "REJECTED",
    ]
```

## 2. 新增运行时目录

```text
scientific_harness/
  events.py
  event_store.py
  sample_state.py
  instrument_state.py
  reconciler.py
  commit_controller.py
  retry_policy.py
  fault_injection/
```

## 3. 禁止盲目重试

每个硬件命令包含：

```text
command_id
idempotency_key
sample_id
protocol_hash
expected_output_path
expected_state_transition
```

超时后：

```text
timeout
→ 查询仪器状态
→ 查询输出文件
→ 核对温度日志
→ 核对样品ID
→ 判断部分完成/已完成/未发生/未知
→ 再决定继续、补偿、隔离或人工处理
```

形式化不变量：

[
\Box
\left[
\operatorname{timeout}(a)
\Rightarrow
\neg\operatorname{retry}(a)
\ \mathcal U
\operatorname{reconciled}(a)
\right]
]

## 4. G1如何使用

G1不增加额外操作，只让Harness在旁路记录：

```text
命令下发
仪器确认
温度日志
原始文件生成
Stage0有效性
是否准入BO
```

G1期间先不让新Harness控制仪器，只比较：

```text
现有系统判定
vs
三重提交shadow判定
```

后续凹凸棒土闭环再启用控制权。

---

# 十五、建立证明携带型科学记忆

当前trial JSON、SHA256和episodic memory是良好基础，但还不能处理“旧证据失效后，下游结论怎样自动降级”。

W3C PROV已定义Entity、Activity、Agent以及使用、生成和派生关系，可以作为基础溯源模型；你们需要在其上增加Claim、Support、Refute和Invalidation语义。([W3C][6])

## 1. 新增SQLite权威图谱

```text
scientific_memory/
  schema.py
  provenance_store.py
  claim_graph.py
  invalidation_engine.py
  retrieval.py
  compactor.py
```

核心表：

```text
entities
activities
agents
claims
evidence_edges
claim_versions
invalidation_events
memory_snapshots
```

## 2. 主张状态采用四值逻辑

```text
UNKNOWN
SUPPORTED
REFUTED
CONTESTED
```

即：

| 支持链 | 反驳链 | 状态        |
| --: | --: | --------- |
|   0 |   0 | UNKNOWN   |
|   1 |   0 | SUPPORTED |
|   0 |   1 | REFUTED   |
|   1 |   1 | CONTESTED |

不能用“最后写入者胜出”。

## 3. 证据失效自动传播

使用已有两个真实历史事件作为测试：

* KK符号bug；
* ECE随机拆分泄漏旧值。

测试过程：

```text
标记旧证据INVALID
→ 删除依赖该证据的支持路径
→ 重算相关参数
→ 重算模型判断
→ 主张自动降级
→ 依赖该主张的旧决策标记为AFFECTED
→ 新检索不得再把旧结果当有效事实
```

验收：

```text
invalidated_evidence_retrieval_rate = 0
downstream_claim_recomputation_rate = 100%
independent_support_preservation_rate = 100%
```

## 4. 多Agent记忆分三层

```text
private_scratch_memory
role_scoped_memory
shared_authoritative_memory
```

权限建议：

| 角色            | 可读        | 可写                          |
| ------------- | --------- | --------------------------- |
| 文献Agent       | 文献、主张图    | Proposed evidence           |
| BO模块          | PRIMARY观测 | 不能修改证据                      |
| 执行Agent       | 已批准协议     | 物理事件                        |
| Claim Agent   | 有效证据      | Proposed claim              |
| 验证器           | 全部只读      | contest/invalidate proposal |
| Policy Engine | 全部        | 权威状态转换                      |

---

# 十六、实现证据保真记忆压缩

不要让LLM自然语言摘要成为长期事实。

## 1. 三层存储

| 层   | 内容          | 压缩策略   |
| --- | ----------- | ------ |
| 原始层 | EIS、日志、事件   | 不做有损压缩 |
| 权威层 | 观测、证据、主张图   | 结构化归纳  |
| 工作层 | Agent上下文、草稿 | 可有损压缩  |

## 2. 压缩产物必须携带证书

```python
class MemorySnapshot:
    summary: str
    supported_claim_ids: list[str]
    refuted_claim_ids: list[str]
    contested_claim_ids: list[str]
    unresolved_questions: list[str]
    hard_constraints: list[str]
    falsifiers: list[str]
    source_hashes: list[str]
    invalidation_frontier: list[str]
    compressor_version: str
    certificate: dict
```

## 3. 压缩回归测试

对每次压缩同时测试：

```text
压缩前后的主张状态是否一致
压缩前后的下一实验决策是否一致
压缩前后的安全边界是否一致
撤销任一关键证据后，失效传播是否一致
是否保留所有反例
是否错误提升主张等级
```

定义：

[
D_{\mathrm{sci}}
================

D_{\mathrm{claim}}
+
D_{\mathrm{action}}
+
D_{\mathrm{counterfactual}}
]

关键快照要求：

[
D_{\mathrm{sci}}=0
]

LLM可以生成阅读摘要，但证书必须由确定性代码产生。

---

# 十七、把现有函数升级成可认证Scientific Skills

不要先增加更多Agent。先把已有确定性模块包装成有合同的Skill。

## 1. 新增Skill基础设施

```text
scientific_skills/
  contracts.py
  registry.py
  compiler.py
  runtime.py
  certification.py
  certificates/
```

## 2. 首批六个Skill

```text
eis-point-analysis@2
arrhenius-sequence-analysis@2
objective-extraction@2
mobo-recommendation@2
llm-strategy-adviser@1
claim-audit@2
```

## 3. Skill合同

```python
class ScientificSkillContract(BaseModel):
    skill_id: str
    version: str
    input_schema: dict
    output_schema: dict
    applicability_domain: dict
    preconditions: list[str]
    postconditions: list[str]
    physical_side_effects: list[str]
    failure_modes: list[str]
    recovery_policy: list[str]
    validators: list[str]
    required_evidence_level: str
    autonomy_level: str
```

例如：

```yaml
skill_id: eis-point-analysis
version: 2.0.0

preconditions:
  - frequency_unit == Hz
  - impedance_unit == ohm
  - len(frequency) >= minimum_points
  - physical_commit in [EFFECT_OBSERVED, RECONSTRUCTED]

postconditions:
  - qa_status is set
  - kk_status is set
  - all_rb_candidates are recorded
  - uncertainty is quantified or explicitly UNKNOWN
  - scientific_admissibility is assigned

physical_side_effects: []
```

## 4. Skill生命周期

```text
DRAFT
→ SIMULATION_VALIDATED
→ SHADOW
→ CANARY
→ VALIDATED
→ DEPRECATED
```

当前可以做到：

* Stage0确定性Skill：通过历史数据和合成测试达到`VALIDATED`；
* MOBO Skill：通过历史回放达到`SHADOW`或`CANARY`；
* LLM策略Skill：保持`ADVISORY`；
* 硬件执行Skill：先在G1中`SHADOW`，后续闭环逐步升级。

## 5. Skill组合检查

```text
eis-point-analysis
→ arrhenius-sequence-analysis
→ objective-extraction
→ mobo-recommendation
```

只有当：

[
\text{postcondition}(S_i)
\models
\text{precondition}(S_{i+1})
]

才允许组合。

例如：

```text
objective-extraction
```

必须要求：

```text
epistemic_commit == PRIMARY_EVIDENCE
```

否则编译器直接拒绝生成闭环工作流。

---

# 十八、主张治理从“禁词扫描”升级为结构化证据门

现有禁词扫描是有价值的最后一道防线，但不能证明语义正确。

## 1. 改名

将：

```text
mechanism_discovery
```

改成：

```text
mechanism_consistency
```

历史字段保留兼容映射。

## 2. 每条主张先生成结构化对象

```python
class ScientificClaim:
    claim_id: str
    claim_type: str
    claim_level: str
    statement_template_id: str
    scope: dict
    support_evidence_ids: list[str]
    refute_evidence_ids: list[str]
    alternative_explanations: list[str]
    falsifiers: list[str]
    effect_distribution: dict
    calibrated_confidence: float | None
    status: Literal[
        "SUPPORTED",
        "REFUTED",
        "CONTESTED",
        "INCONCLUSIVE",
        "INVALID",
    ]
```

自然语言报告只能从这个对象渲染。

## 3. 风险—覆盖曲线替代“恒HOLD”

当前v2恒HOLD只能证明fail-closed，不能证明能够正确放行。

建议在历史主张集上计算：

```text
coverage = 被自动放行的主张比例
risk = 被放行主张中的错误支持率
```

选择性预测的核心就是通过拒答降低风险，同时报告覆盖率；不能只报告拒答后风险很低。([NeurIPS Papers][7])

建议权限：

| 主张等级  | 自动化            |
| ----- | -------------- |
| C0/C1 | 确定性规则可自动       |
| C2    | 校准风险达标后自动或人工抽查 |
| C3    | 必须前瞻登记和人工确认    |
| C4    | 必须独立证据审查和人工批准  |
| C5    | 当前系统禁止         |

Agent置信度使用Brier、log loss或CRPS等严格适当评分规则；这类评分能够激励报告真实概率，而不是通过夸大置信度争取更多权限。

---

# 十九、补齐回放和消融，不增加任何物理实验

新增：

```text
evaluation/
  observed_candidate_replay.py
  policy_ablation.py
  prompt_perturbation.py
  fault_injection.py
  claim_risk_coverage.py
  skill_certification_benchmark.py
```

## 1. BO回放必须使用时间顺序

推荐两种严格区分的回放：

### Observed-candidate replay

每轮只能从“后来真实测过的剩余候选”中选择。

优点：

* 结果来自真实实验；
* 不需要虚拟oracle。

局限：

* 只能评价已观测候选集合中的选择效率。

### Model-based replay

使用拟合表面生成虚拟结果。

必须标记：

```text
synthetic_not_real_evidence
```

不能作为真实BO优越性的核心证据。

## 2. 对照策略

```text
random
LHS
legacy ParEGO
noise-aware qLogNEHVI
hard-constrained MOBO
deterministic projection
LLM Top-K selector
full governed system
```

每个策略：

* 固定相同初始点；
* 多随机种子；
* 配对bootstrap；
* 报告置信区间；
* 报告最坏种子；
* 报告违规和无效观测。

## 3. LLM稳定性

在不改变科学输入的情况下进行：

* 提示顺序扰动；
* 同义改写；
  -无关字段插入；
* 文献顺序变化；
* 模型重复调用。

评价：

```text
recipe_selection_stability
constraint_violation_rate
acquisition_value_loss
fallback_rate
unjustified_delta_rate
```

---

# 二十、CI和验收矩阵

| 模块      | 必须通过的验收                |
| ------- | ---------------------- |
| Rb分析    | 所有方法均有明确成功/失败原因        |
| 路由伪影    | 无转变合成数据全管线FPR被量化       |
| 断点      | 有bootstrap区间、LOO和方法敏感性 |
| 单调性     | 无任何点仅因趋势不符被删除          |
| KK准入    | KK失败数据进入BO次数为0         |
| 不确定度    | 所有BO观测均有方差或明确UNKNOWN   |
| 复现地板    | 每个地板均携带scope和CI        |
| 目标函数    | 所有trial均携带目标定义哈希       |
| BO      | legacy与v2可并行回放         |
| LLM     | 有确定性投影和规则修复基线          |
| Harness | 超时后盲目重试次数为0            |
| 记忆      | 旧证据作废后下游自动重算           |
| 压缩      | 关键证据撤销前后结果等价           |
| Skills  | 控制面只能调用VALIDATED Skill |
| 主张      | C3/C4不能由LLM自行升级        |
| 溯源      | 从主张可追到原始谱、代码和Skill版本   |

---

# 二十一、建议实施顺序

## 第一阶段：直接影响当前科学结论

1. 冻结legacy系统；
2. Rb全方法并行与方法敏感性；
3. 取消单调性删除；
4. KK和证据准入重构；
5. 谱级bootstrap与断点区间；
6. 无转变/注入转变全管线模拟；
7. 实践可辨识性报告；
8. 目标函数注册表。

这部分必须先完成，因为它决定现有“亚零度转变”和“可辨识性边界”是否对分析方法稳健。

## 第二阶段：在G1前冻结

1. 复现地板层级模型；
2. Stage0不确定度输出；
3. 噪声感知MOBO；
4. LLM确定性基线；
5. 三重提交shadow模式；
6. G1分析配置和Skill版本冻结。

## 第三阶段：利用G1，不增加额外操作

G1同时完成：

* 凹凸棒土局部直接复现地板；
* 新Stage0双轨分析；
* 三重提交shadow验证；
* EIS Skill canary验证；
* 新噪声模型初始化。

## 第四阶段：后续凹凸棒土闭环

启用：

* 噪声感知qLogNEHVI；
* 新配方/重复动作选择；
* 三重提交控制；
* Scientific Skill运行时；
* 主张证据图；
* 动态停止条件。

## 第五阶段：Agent基础创新

1. 证据失效传播；
2. 多Agent权限记忆；
3. 反事实记忆压缩；
4. Skill证书；
5. 风险—覆盖主张门；
6. 动态自治。

---

# 最终建议的项目主张

完成上述代码修改后，你们不应把核心贡献表述为：

> “Agent找到了性能更好的材料。”

更强、也更可信的表述是：

> **建立了一个面向部分可观测、不可逆、高成本湿实验的证据治理型Scientific Agent运行时；该系统能够区分软件执行、测量有效和科学证据，量化分析管线自身的假发现风险，传播证据失效，并仅允许经过认证的Skill和校准主张进入真实闭环。**

三个可以真正成立的底层贡献将是：

[
\boxed{
\text{分析伪影可检验}
}
]

[
\boxed{
\text{无效证据不可泄漏}
}
]

[
\boxed{
\text{证据失效可传播，Skill权限可撤销}
}
]

G1和后续凹凸棒土闭环只负责提供真实环境下的落地验证；其余绝大部分问题都可以通过历史数据重算、合成数据、软件故障注入、时间顺序回放和结构化代码改造解决。

[1]: https://www.nist.gov/programs-projects/development-standards-support-modular-and-autonomous-laboratory-ecosystem "Development of Standards to Support a Modular and Autonomous Laboratory Ecosystem | NIST"
[2]: https://pmc.ncbi.nlm.nih.gov/articles/PMC10288619/?utm_source=chatgpt.com "Electrochemical Impedance Spectroscopy—A Tutorial - PMC"
[3]: https://www.nature.com/articles/s41529-024-00537-8 "Bayesian assessment of commonly used equivalent circuit models for corrosion analysis in electrochemical impedance spectroscopy | npj Materials Degradation"
[4]: https://botorch.org/docs/models "Models | BoTorch"
[5]: https://botorch.org/docs/tutorials/constrained_multi_objective_bo?utm_source=chatgpt.com "Constrained multi-objective optimization with qNEHVI and ..."
[6]: https://www.w3.org/TR/prov-dm/ "PROV-DM: The PROV Data Model"
[7]: https://papers.neurips.cc/paper/7073-selective-classification-for-deep-neural-networks.pdf?utm_source=chatgpt.com "Selective Classification for Deep Neural Networks"
