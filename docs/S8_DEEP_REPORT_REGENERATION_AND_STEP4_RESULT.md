# S8 深度机理报告备份与重新生成、step4 对照结果

**日期**：2026-01-30  

**说明**：可选方法（可接受范围）已放弃并复原；**当前采用方案 A/B**（见 `ML_AI_VALIDATION_OPTIONS.md`）。以下记录仅作历史参考。

---

## 一、备份

- **备份文件**：`close/output/deep_analysis/S8_deep_mechanism_analysis_bak_20260130.md`
- **内容**：重新生成前的 S8 深度报告（含此前手写添加的「可接受范围」两行；生成时间 15:46:25）。
- **恢复方式**：若需恢复，将上述备份覆盖当前 `S8_deep_mechanism_analysis.md` 即可。

---

## 二、重新生成

- **脚本**：`close/phase2/run_s8_deep_analysis.py`（已包含第五章「可接受范围」输出要求）。
- **新报告**：`close/output/deep_analysis/S8_deep_mechanism_analysis.md`，生成时间 **21:59:32**。
- **格式差异**：
  - 新报告第四部分采用 **「预测最优配比：R = r1-r2，N = n1-n2」**（区间格式），未再输出「R = x ± dx，N = a ± da」。
  - 新报告中 **未出现**「可接受范围 高温/低温 R∈[...], N∈[...]」两行（LLM 未按提示词输出该格式）。

---

## 三、step4 解析扩展

为兼容新报告，step4 已支持解析 **「R = r1-r2，N = n1-n2」** 格式（`_parse_r_n_range_line`），与原有「R = x ± dx，N = a ± da」并存。新报告可被正确解析为：

- **高温**：R∈[0.4, 0.6]，N∈[3.0, 4.0]
- **低温**：R∈[0.6, 0.8]，N∈[2.0, 3.0]

---

## 四、对照结果（能对照上吗？）

| 项目       | 备份报告（含手写可接受范围） | 新生成报告（核心推荐 R=0.4–0.6, N=3.0–4.0 等） |
|------------|------------------------------|-----------------------------------------------|
| **区间来源** | 可接受范围                   | 核心推荐（R = r1-r2 格式）                    |
| **高温区**   | n_in=20, n_out=23；mean_Ea 内 0.159、外 0.191；p≈0.33 | **n_in=0**, n_out=43 → **SKIP**（区间内无数据） |
| **低温区**   | n_in=0                       | **n_in=0**, n_out=39 → **SKIP**               |

**结论**：

- **能解析**：新报告可被 step4 正确解析，区间与报告第四部分一致。
- **对照不上**：新报告给出的核心推荐区间（高温 R∈[0.4,0.6]、N∈[3.0,4.0]；低温 R∈[0.6,0.8]、N∈[2.0,3.0]）与当前 S8 数据分布**无重叠**（两温区区间内 n_in 均为 0），无法做区间内/外 mean(Ea) 的统计对照。
- 若希望继续做“可接受范围”下的对照（高温 n_in=20、p≈0.33），可 **恢复备份** 使用原报告，或在新报告中手写补上「可接受范围」两行后再跑 step4。

---

## 五、建议

1. **保留备份**：`S8_deep_mechanism_analysis_bak_20260130.md` 建议保留，便于对比或恢复。
2. **正文表述**：无论用新报告还是备份报告，ML–AI 部分均建议采用「相容、数据覆盖有限」的弱化表述；详见 `docs/ML_AI_VALIDATION_OPTIONS.md`。
3. **提示词**：若希望新生成的报告自动输出「可接受范围」格式，可在第五章中进一步强化格式示例与“必须另起一行写出可接受范围”的说明，再重跑 `run_s8_deep_analysis.py`。
