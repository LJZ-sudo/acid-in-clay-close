# 发表就绪度评估（PUBLICATION READINESS，现状快照：2026-07-05）

> 本文件是**当前状态的就绪度判断**(自评,非录用保证)。执行层任务卡/排期见 `../OPTIMIZATION_EXECUTION_PLAN_20260622.md`;架构与真实 `file::function` 见 `../SYSTEM_ARCHITECTURE_CODE_GROUNDED_20260622.md`;现状家底见 `../PROJECT_SITUATION_REPORT_20260622.md`。
> 本版为**重写版**:合并了此前所有分日期的增补层,只保留截至 2026-07-05 的真实结论(含 H 系列旧材料硬化 H1–H4)。口径为严格真实成熟度——**"软件 Demo 通过"不等于"已完成贡献",档次由真机证据决定**。历史 run 用 run-id 标识。

---

## 0. 双轨定位（最重要，先读）

本项目是一个 **EIS-only、表征受限条件下的"证据治理型半闭环自主发现"** 项目,同时承载两条都必须做、互相支撑、不可替代的工作:

| 轨 | 论文/贡献定位 | 现状 | 目标刊 | 必须做 |
|---|---|---|---|---|
| **A 轨 — 材料/EIS/治理主线** | EIS-only、低数据下自主发现的**复现下界 ≤ 可分辨效应 ≤ 可声称结论强度**案例 | 接近投稿前收口(分析门清 + G1 主门过,缺写作 + 续测新 R/N) | **Tier B**(Comm. Chem./Mater./CRPS/npj);DD/MLST 保底 | ✅ |
| **B 轨 — Scientific Agent 方法学** | 可信科学 Agent 运行时:三重提交 Harness / 证明携带记忆 / 可认证 Skills + ESAS-OS 2.0 + Epistemic-OS | 软件全体系落地 + 真机治理路径已验证(G-2/G-4/G-5 门) | 潜力 **Tier S / 旗舰方法学** | ✅ |

> **铁律**:A 和 B 不是二选一,并行推进、各自深化。A 瓶颈是实验台时间(G-1/G-3),B 主要纯代码,天然可并行。合并成一篇还是拆两篇是**发表策略问题**(§6),不影响"两件工作都要做扎实"。

---

## 1. 统一脊柱（A、B 共用的论文主线）

\[ \textbf{可信自治上限} = \min(\text{复现能力},\ \text{可辨识能力},\ \text{执行可靠性},\ \text{证据治理能力}) \]

- **复现能力**(复现地板)→ "多小的提升不可信";
- **可辨识能力**(模型混淆)→ "多强的机制主张不可支持";
- **执行可靠性**(B 轨 SciTX)→ "哪些测量有资格成为证据";
- **证据治理能力**(B 轨 E-Mem/PC-Skills)→ "失效证据是否被传播、主张是否被过度声称"。

A 轨给出前两项(观测与统计边界),B 轨给出后两项(执行与治理边界)。**缺任何一项,这条不等式都不闭合——这是 A、B 必须都做的根本原因。**

---

## 2. 就绪度评分（1–5，5=可投；A/B 分开打）

| 轴 | A 轨 | B 轨 | 依据 |
|---|---|---|---|
| 科学问题定位 | **4.5** | **4** | "复现地板↔可辨识天花板↔主张封顶"是真方法学贡献,不是材料数字 |
| 真实基础 | **4** | **3.5** | A:多批真实 EIS + 线 B 同配方直接地板;B:411 pytest collected + **4 次真机全温区 live run(治理/逐点 LLM/收尾推理均真跑)+ G-2/G-4 真机执法** + H 系列 ③④⑤⑥ 软件实现并离线真验,但**仍同一物理片、H2/H3/H4 live 终验与多批互证未做** |
| 技术稳健性 | **4** | **3.5** | A:M0–M2 分析门已落地;B:跨层失效闭环 + 真机 enforce/canary 执法 + 故障注入拦截 + C³ 主循环消费(H1)+ 在线仪器见证(H2)+ R4 激活模式(H4),余 live 终验/多批 |
| 原创性 | **3.5** | **4(潜力)** | A:组合视角新;B:三提交/反事实失效/双账户/认知证书/证伪市场在 EIS 场景有独特价值 |
| 主张诚信 | **4.5** | **4.5** | C0–C5 封顶 C4、负结果不藏;B 已诚实标"未激活 R4/同片重复/C³ 不消费" |
| 系统整合度 | — | **3.5** | B 轨"Skill→事务→证据→记忆→BO→GP 重训 + LLM 大脑 + 认知证书"互联;余多批 + R4 激活 |

> 解读:**A 轨(证据治理案例)= 中上,接近可投;B 轨(Agent 基础研究)= 软件全体系 + 真机治理路径已验证**,距方法学顶刊还差**多批 live 互证 + 新 R/N + R4 人审激活**(§4.4)。

---

## 3. A 轨就绪度（Tier B 案例研究）

### 3.1 已落地的分析门（M0–M2，纯代码，真实结果可复算）

| 门 | 内容 | 真实结果 | 状态 |
|---|---|---|---|
| G2 | Rb 方法不变性 + 合成全管线 FPR | 主力 LRS risk=low;6.15_merged FPR=0.06;弱数据集 0.14–0.24 诚实标注 | 🟩 |
| G7 | 评分口径注册表(训练 vs 审计隔离) | `combined_score` 与 `audit_score_v3` 各带冻结 sha256 | 🟩 |
| G8 | 复现地板层级方差 | between-specimen 0.252 dex;分析不确定度~0.058 ≪ 地板 | 🟩 |
| **G1/E1 直接地板** | 线 B 体系自身复现地板(同配方 3 片) | `LINE_B_LOCAL_DIRECT`:WARM median 0.106/p90 0.146 dex(<代理 0.262)、COLD median 0.158/**p90 0.356 dex(>代理)**;三批相变温度一致(256/254/257K、226/222/227K) | 🟩(真机)。**冷段更松须写进 Discussion** |
| (可辨识) | 模型混淆矩阵 | 三律正确判别 0.55–0.73 → design-conditional partial non-identifiability,封顶 C4 有据 | 🟩 |
| G4 | LLM 安全 vs 5 确定性基线 | 确定性规则可复制 LLM 把 R 拉回观测域 → LLM 安全价值"可替代"(诚实下调叙事) | 🟩 |

### 3.2 投稿前剩余门槛

| 门 | 内容 | 状态 |
|---|---|---|
| **G1 主门(同配方重复)** | 线 B 最佳点 R=0.186/N=1.029 同配方 ≥3 独立片 | 🟩 **已完成**:3 片 + trial1 = 4 次独立重复 → `LINE_B_LOCAL_DIRECT` |
| G1 续测(新 R/N) | R=0.15、R=0.12(低 R 边缘探索) | 🔴 **未做**:属新 R/N,须先经官方 MOBO+LLM 冻结→push→才合成(前瞻纪律) |
| 写作 | M0–M2 v2 结果 + G1 直接地板写进 Methods/Results/SI | 🔴 未做 |
| G3 | 闭环对照全程标注 replay vs 真实前瞻 | 🟡 分析已分层,待手稿统一措辞 |
| G5 | 校准 bootstrap CI + leave-one-family-out;图矢量化/SI | 🟡 出版工程 |

> **G1 诚实边界**:n=3(+trial1=4)是 **direct preliminary reproducibility estimate**,n 小、区间宽,非大样本稳定地板;但足以去掉 Fig 3 生物聚合物代理 caveat。**关键诚实发现**:暖段直接地板(0.146)比代理(0.262)紧,但**冷段 p90 0.356 超过代理**——"候选差 vs 地板"冷段判据更严,须在 Discussion 记"低温复现性更差"。这三片为**回溯重复**;续测新 R/N 须前瞻冻结后合成,不混入回溯地板。

---

## 4. B 轨就绪度（Tier S 方法学）

### 4.1 已完成（软件层，真实可复算，411 pytest collected）

| 创新 | 实现 | 关键验收 |
|---|---|---|
| **SciTX Harness** | 多见证 C_P + C_M(用途)/C_E(主张)(U1–U6)+ `EvidenceTransaction` + `ActionGate` 旁路清零 | blind_retry=0 / ¬C_P⇒全拒 / autonomous_bypass=0 |
| **E-Mem** | 四值超图 + 失效传播 + 快照写门 + 根去重 + 失效→BO 重建→**GP 重训** + 压缩证书 | 失效→best 变化可查;压缩破坏即拒证 |
| **PC-Skills** | 6 真实 Skill + 三层证书(Wilson 下界,provisional)+ 漂移/撤销 + 双账户 | 证书由检查产生不可手填;自信≠执行权 |
| **端到端演示** | 故障→拦截→撤销→BO 重建→下一动作变;B0/B2/B4/B5 真实臂 | governed 严格优于 ungoverned |
| **ESAS-OS 2.0 四插件** | 测量事务化 / Rb-ACT / R²-Memory(拦截 100%)/ C³-Harness(停⊆legacy 停) | 默认 shadow/旁挂,legacy 永不覆盖 |
| **Epistemic-OS** | 认知证书(σ 层)/ 阻抗正问题(谱级)/ 证伪市场(多角色 LLM)/ active_design | 均接 live 收尾;fe4b 真机佐证齐全 |
| **LLM 大脑接闭环** | 逐点 agent(R²-Memory+C³+active_design 注入 prompt)+ 收尾机制推理链 | 真调 gpt-5.4;numpy 序列化 bug 已修并复验 |
| **H 系列旧材料硬化(2026-07-05)** | ①C³ 证书**被主循环消费**(单调安全)②在线**仪器见证**+协议级故障注入 ③canary **多驱动**(±2 step)④Rb-ACT **R4 激活模式**(三条件人审门+σ_v2+delta) | 全用**真实数据离线真验**:`pH1` 11/11(真 history_db replay)、`pH2` 17/17(真 EIS 文件)、`pH3` 7/7、`pH4` 6/6(**87 真谱**,Spearman=0.995、legacy_overwritten=0);77 pytest 无回归。**live 终验待旧材料 run** |

### 4.2 B 轨真机硬门现状（决定档次的硬证据）

| 门 | 状态 | 真机证据(run-id) |
|---|---|---|
| **G-2 真机故障注入对照** | 🟢 已执行 | fe4(`run_20260702_093757_f7a893`):2 类故障六用途全 REJECT、all_caught=true、温控/CHI 未受影响;`p20` 10/10。**协议/见证类故障(ACK/仪器卡住/文件/条码/校准)现已软件实现并离线真验(H2,`pH2` 17/17,真 EIS 文件),live 终验待旧材料 run;物理破坏性故障仍须 dummy cell** |
| **G-4 enforce/canary 真机长跑** | 🟢 已执行 | fe4b(`run_20260702_144507_52a878`,33 点):enforce 拒 6 深冷点出 BO(27/6)、canary 2 次物理执行、bypass=0;`p21` 10/10。**H3 已把 canary 邻域放宽到 ±2 step 让更多步实质微调(`pH3` 7/7),live 终验待旧材料 run** |
| **G-5 Rb-ACT R4 多批审计** | 🟡 门全过,替换未激活 | fe2+fe3+fe4b 合并 87 真谱/75 配对点,四门全过 `gates_pass=true`;`p18` 12/12。**H4 已实现 R4 激活模式(三条件人审门+σ_v2+delta+"BO 不劣化"守卫),离线用 87 真谱验:签核后 σ_v2 全产出、Spearman=0.995、`legacy_overwritten=0`、`bo_not_degraded=true`(`pH4` 6/6)。真机替换态 run 须你的签核 token;跨批多片仍属 G-5** |
| **G-1 新 R/N 前瞻闭环** | 🔴 未做 | 须前瞻冻结后合成 |
| **G-3 多批 live 互证** | 🔴 未做 | 4 次 live run 全同一物理片=同片重复,须 ≥3 独立片 |

> **逐点 LLM agent 忠实度(重要诚实)**:fe3/fe4b 期间 numpy 序列化 bug 致 n≥11 步静默回落规则(fe3 25/35、fe4b 23/33)、`llm_called` 误报。已修并在 llmfix run(`run_20260703_193436_ba1b26`)复验:0 序列化失败、n≥11 步 11/11 真 LLM。历史 run 测量/治理数据不受影响。

### 4.3 距 Tier S 还差什么（必须做，不能用"软件通过"预支）

软件层的权威语义、跨层失效闭环、端到端演示、认知证书、LLM 大脑均已落地,且真机治理路径已收 G-2/G-4/G-5 三门。**H 系列(2026-07-05)已把原先列为局限的 ③④⑤⑥ 全部在旧材料上软件实现并离线真验**(C³ 主循环消费、在线仪器见证+协议级故障注入、canary 多驱动、R4 激活模式)。**剩余硬门(须新实验或真机 live,不能用软件预支)**:
1. **G-1 新 R/N 前瞻闭环**:须官方 MOBO+LLM 冻结→push→合成→全温区 live。**须新材料**。
2. **G-3 多批(多片)live 互证**:现 4 次 live run 全同一物理片,须 ≥3 独立制备片,证书才能去 provisional。**须多片**。
3. **G-5 R4 生产激活(跨批)**:激活模式已实现且离线真验(H4);须你签核 `rb_r4_activate=true`+token 跑一次替换态真机 run 验 live"BO 不劣化",跨批多片互证仍属 G-5。
4. **G-2 物理破坏性故障**:协议/见证类(ACK/校准/文件/条码/仪器卡住)已软件实现并离线真验(H2);仅**电极短接/断路等物理破坏性故障**须 dummy cell,不在旧样品做。
5. **H2/H3/H4 live 终验**:软件已离线真验,须旧材料一次全温区 live 做 live 终验(不改代码/不伪造)。

---

## 5. 主张边界（可声称 vs 不可声称，务必写进手稿）

| 可声称 | 不可声称 |
|---|---|
| "实现了三提交证据准入可执行原型,并在真机 live 完成 shadow/enforce 接口 + 故障注入拦截" | "已在多批独立样品上验证证据事务 Harness" |
| "实现了主张—证据超图与反事实失效传播原型,并接进 live 收尾" | "已实现经多批真机验证的多 Agent 科学记忆" |
| "实现了 Skill 合同/证书/生命周期/双账户治理 + Rb-ACT 预注册审计" | "已用 Rb-ACT 值替换 legacy 进入生产数值链"(R4 未激活) |
| "human-supervised、evidence-governed 的半闭环系统" | "完全自主的自驱动实验室" |
| 描述性"传导区间转变 / conductivity breakpoint" | "相变 / 证明了某微观传导机制"(EIS-only 封顶 C4) |

---

## 6. 发表策略（合并 vs 拆分）

- **首选拆两篇**:A 轨(真实实验+复现地板+可辨识上限+治理闭环)先投 Tier B;B 轨(SciTX+E-Mem+PC-Skills+ESAS-OS+Epistemic-OS,以 A 轨为真实案例)冲 Tier S。
- **合并冲 Tier S 的前提**:§4.3 的真机硬门(尤其 G-3 多批 + G-1 新 R/N)齐备,且"可信自治上限"统一式真把材料案例与 Agent 运行时缝成一条线;否则会被审稿人当"多条故事平铺"。
- **无论合并还是拆分,A 和 B 都要做扎实**——这是发表形式的选择,不是"做不做"的选择。

---

## 7. 关键风险与诚实局限

**A 轨**:① 线 B 地板 n=3(预备估计)且冷段(p90 0.356)比代理松,低温复现性差须如实写进 Discussion;② 复现感知是负结果(单曲线 QC 测不准复现);③ 闭环轨迹短(不声称收敛);④ 材料类不新(新意全在转变+治理+边界);⑤ 机理被自身可辨识性封顶 C4。

**B 轨(2026-07-05 更新:③④⑤⑥ 经 H 系列已软件实现并离线真验,其余为须新实验/真机的真局限)**:
- ① 4 次 full-temp live run 全同一物理片=同片重复,**无多批 live 互证**(G-3,须多片);
- ② 新 R/N 未合成(G-1,须新材料);
- ③ Rb-ACT R4:**已实现激活模式**(三条件人审门+σ_v2+delta,`rb_act/activation.py`),离线用 87 真谱验 `legacy_overwritten=0`/Spearman=0.995/`bo_not_degraded=true`(`pH4` 6/6);**真机替换态 run 待你签核 token,跨批生产激活仍属 G-5**;
- ④ C³-Harness 收敛证书:**已从"只写盘"升级为"主循环消费"**(`termination_evaluator` 新增 `verdict_effective`/`c3`,单调安全 c3_stop⊆legacy_stop;live `_finalize_c3` 落 `c3_evidence.json` 供 Stage1 聚合消费),`pH1` 11/11 真 replay 验证;
- ⑤ 仪器见证类故障:**已实现在线 `InstrumentWitness` + 协议级故障注入**(ACK/仪器卡住/文件/条码/校准,`instrument_witness.py`),`pH2` 17/17 真 EIS 文件验证;**仅物理破坏性故障(短接/断路)仍须 dummy cell**;
- ⑥ active_design canary:**邻域已放宽 ±1→±2 step 让更多步实质微调**(`canary_max_steps`,`pH3` 7/7);仍不做无人值守 enforce 夺权;
- ⑦ 统计证书小样本 provisional(须真机成功率累积);
- ⑧ **H2/H3/H4 的 live 终验待旧材料一次全温区 run**(软件已离线真验,live 只做终验)。

**P0(WP0)已完成**:13/15 数据集口径澄清(`dataset_registry.yaml`)· 目标函数收口(G7 漂移守卫接主闭环)· 硬件写路径审计旁路清零 · claim 层 `mechanism_consistency` 别名(全仓 `phase_transition` blanket 改名故意不做,低价值 churn)· `LLMCallBundle`(Fernet 加密,公开侧只发 sha256)。

---

## 8. 一句话结论

> **A 轨(证据治理案例)**:G1 主门已过(同配方 3 片 → 直接复现地板),余写作 + 续测新 R/N + 措辞收口 → 稳投 Tier B。**B 轨(Agent 方法学)**:软件全体系(WP0–WP5 + ESAS-OS 2.0 四插件 + Epistemic-OS + LLM 大脑接闭环 + H 系列旧材料硬化)已落地,411 pytest collected 全覆盖,并在真机 live 回路验证治理/逐点 LLM/收尾推理/认知证书;**H 系列(2026-07-05)已把原先列为局限的 ③C³ 主循环消费、④在线仪器见证+协议级故障注入、⑤canary 多驱动、⑥R4 激活模式全部在旧材料上软件实现并离线真验**(`pH1`–`pH4` 全过、77 pytest 无回归)。距 Tier S 的真机硬门已收 **G-2🟢 + G-4🟢 + G-5🟡**,**剩余 = G-1 新 R/N 合成(须新材料)、G-3 多片 live 互证(须多片)、G-5 R4 跨批生产激活(须你签核)、G-2 物理破坏性故障(dummy cell)、H2/H3/H4 的 live 终验**——这是必要条件,不能用"软件 Demo/测试通过"预支。两轨都必须做、并行推进、持续深化。
